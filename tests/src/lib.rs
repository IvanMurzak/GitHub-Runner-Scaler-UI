// owner: h1-e2e-security-acceptance
//
// Acceptance scenarios and security gates live in `tests/tests/*.rs` and are
// marked `#[ignore]`, so the `e2e` CI job's fixed command
// `cargo test -p runner-manager-e2e -- --ignored` is what selects them and a
// developer running `cargo test --workspace` never reaches real GitHub.
