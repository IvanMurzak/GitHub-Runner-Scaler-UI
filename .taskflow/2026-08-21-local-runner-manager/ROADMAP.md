# Local Runner Manager implementation ledger

> This is this taskflow's implementation ledger, not a workspace-wide product
> roadmap.

**Design status:** Re-reviewed 2026-08-21 (`/taskflow-review`) — **D4 REVISED**
**Task status:** Re-derived 2026-08-21 (`/taskflow-tasks`) against the corrected
design. 23 immutable specifications in [`tasks/`](tasks/), 9 conflict-domain
groups, waves 0-4.
**Implementation status:** **Stopped 2026-08-22 at the owner's request; nothing
in flight.** 16 of 23 tasks done. Today's run (`--parallel=6 --review=medium
--scope=all --merge=on-green`) merged `c4`, `d2`, `e1`, `f1` and `e2`, and fixed
`main`, which had been red on every push since `a3` merged. `d3` is complete and
held at `🟣` — green on all seven checks, but it is the one task that has not
had a review round. **Resume by reviewing `d3`, then dispatching `e3`, `f2` and
`g1`, which are three ready group heads and the widest this graph gets.** The
constraints their briefs must carry are in the last progress-log entry.
**Last updated:** 2026-08-22

**Execution host:** this repository, `IvanMurzak/GitHub-Runner-Scaler-UI`. The
transfer from `ai-pipeline` is complete, so `/taskflow-execute` runs here
against an empty Rust workspace.

Immutable task specifications live in [`tasks/`](tasks/); its
[README](tasks/README.md) carries the coefficient legend, the model rubric, the
group-to-path map, and the record of what the D4 revision changed.
**This file is the only mutable record of task state.**

## Execution timeline

| Wave | Phase | Outcome | Gate |
|---|---|---|---|
| 0 | P0 | Rust workspace, `rust-toolchain.toml`, committed `Cargo.lock`, matrix CI on PR and `main` (D10), release-workflow skeleton, and the deterministic domain model with its persistence. None of these exist today; `HEAD` carries only `LICENSE`, `.gitignore`, this taskflow, and the D17 spike record. | Satisfied 2026-08-21: name is `runner-manager` (human gate 1). |
| 1 | P0 | Organization-scope `generate-jitconfig` proven (`v1`). Then device-flow onboarding (D3), machine-scoped token storage, platform primitives, read-only REST inventory with the shared request budget, and the full host and policy configuration CLI including both capacity levels (D9) and monitor-only policies (D19). Nothing scales yet: `add` never arms a host (D20). | Published App registered and its permissions reviewed with the `Administration: Read and write` consequence accepted (human gate 2). Organization-scope `generate-jitconfig` proven by `v1`. |
| 2 | P1 | REST demand polling at repository and organization scope (D18), JIT ephemeral runner lifecycle, runner package cache, both capacity ceilings enforced, cleanup, local logs, the service installers, and the `daemon`/`service` command surface. | One-repository Windows pilot succeeds, including boot-start recovery (human gate 3). |
| 3 | P2, P3 (macOS) | Ratatui keyboard/mouse dashboard, host and policy settings screens with CLI parity, accessible error states, and macOS validation of the Wave 2 service installers. | Windows and macOS acceptance journeys pass (human gate 4). |
| 4 | P3 (Linux), P4 | Linux validation, the end-to-end and security acceptance suite, the full release workflow, install scripts, distribution channels, README, and public beta. | Security, offline, and cross-platform gates pass; rollback drill executed per OS (human gate 5). |

Phase identifiers refer to `06-migration-rollout.md`.

**Three deviations from the original wave text, recorded deliberately.**

1. All three service installers ship in Wave 2 as `d3`, not in Wave 3, because
   gate 3 requires verified boot-start recovery on Windows and there is no
   honest way to deliver that without the installer. Wave 3 and Wave 4
   *validate* the same implementation on macOS and Linux; no second
   implementation task exists.
2. `f2` — the `repo` and `org` command families — is in Wave 1, not Wave 2.
   After D4 `add` creates nothing remotely and arms nothing, so the entire
   configuration surface is buildable before the agent exists. Wave 1 therefore
   ends at "everything is configurable and nothing runs", which is exactly what
   human gate 2 needs to approve.
3. `v1` runs in Wave 1 rather than being folded into `c4`. It is the same class
   of task as `c1`: a cheap experiment that decides whether a design path
   exists. Running it before the org code is written is the lesson `c1` already
   taught at a cost of one full design revision.

## Waves as tasks

```text
Wave 0   a1 ──┬── b1 ── b2
              │
Wave 1        ├── v1                                  (org JIT proof, no deps in code)
              ├── c1 ✅ ── c2 ── c3
              ├── d1 ── d2
              └── (c3 + d2 + b2) ── f1 ── f2

Wave 2   (c3 + b1 + v1) ── c4 ── e1 ─┬── e3 ── f3
                            e2 ──────┘
                     (d1 + b2) ── d3 ──────────┘

Wave 3   f1 ── g1 ── g2 ── g3

Wave 4   a1 ── a2 ── a3
         (f3 + g3 + d3 + v1) ── h1
```

Parallelism the graph allows: groups **V**, **C**, **D**, and **B** run
concurrently from Wave 0/1; **F** starts as soon as `c3`, `d2`, and `b2` land;
**A**'s release work (`a2`, `a3`) is independent of every product group and is
scheduled last only because it publishes.

## Human-approval gates

Dispatch stops at each gate until the owner records GO in the progress log.

| # | Gate | Blocks |
|---|---|---|
| 1 | Satisfied 2026-08-21. Repository identity is settled (`IvanMurzak/GitHub-Runner-Scaler-UI`, public, MIT) and the crate, binary, and package name is `runner-manager`. | — |
| 2 | **GO recorded 2026-08-21.** Approve the **published** App's permission set — including the `Administration: Read and write` consequence in `07-security.md`, which every future user inherits — and its configuration (device flow on, token expiration opted out, no private key, no webhook). One-time and product-wide, not per user. Approve on the evidence from `v1` that organization-scope `generate-jitconfig` works. **No longer blocked by D17:** the spike is green. | `c2` and everything downstream of it |
| 3 | **GO recorded 2026-08-23.** The owner approved measured `host_capacity=1` and repository policy `max_capacity=1`. A post-reboot Windows pilot proved boot-start before the interactive shell, successful GitHub contact, exactly one ephemeral runner, both ceilings enforced, a successful repository workflow job, and complete terminal cleanup. | Wave 2 exit |
| 4 | Approve retirement of legacy persistent runners only after the autoscale pilot has completed a representative workflow. | Wave 3 exit |
| 5 | Approve public release after the security, offline, and cross-platform gates in this taskflow pass. | `a2`, `a3`, and the release rehearsal |

Two further human actions are prerequisites rather than gates, and both are
irreversible-adjacent, so `/taskflow-execute` must confirm them before
dispatching the task that needs them:

- **Before `v1` is dispatched:** an App with `Organization → Self-hosted
  runners: Read and write`, installed on a disposable organization. The
  throwaway App `runner-manager-d17-spike` already satisfies this. **Run `v1`
  before deleting it**, then delete it — the ROADMAP previously called for
  immediate deletion, and doing that first would make `v1` wait for human
  gate 2 for no reason.
- **Before the release task's first real dispatch:** the release workflow
  publishes public artifacts under the project's name. Rehearse against a
  pre-release version before any real one.

## Board

23 tasks. `/taskflow-execute` is the only writer of the Status, Run / PR, and
Updated columns.

**Progress: 17 of 23 done** — `a1`, `a2`, `a3`, `b1`, `b2`, `c1`, `c2`, `c3`, `c4`, `d1`, `d2`, `d3`, `e1`, `e2`, `f1`, `v1`, and
`v2` (an unplanned verification spike, recorded below rather than as a row since
it has no specification in `tasks/`). Group A is complete. **Correction, 2026-08-22:** the claim that
previously stood here — that `main` carried 295 passing tests with CI green on
Windows, macOS and Linux — was false, and had been since `a3` merged.
`cargo test --workspace` failed on **every** push to `main` from `5fd95dad`
onward, on all three legs. It is true again as of PR
[#2](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/2) (`9f39ffd`);
see the log below. Merges are no longer all local either: that PR is the first
worker branch to reach `origin`.

| Task (spec) | needs | imp/cx | model | Status | Run / PR | Updated |
|---|---|---|---|---|---|---|
| **Wave 0** | | | | | | |
| [a1-workspace-ci-foundation](tasks/a1-workspace-ci-foundation.md) | — | 10/5 | mid | ✅ done | `b67ca43` (local merge) | 2026-08-21 |
| [b1-domain-core](tasks/b1-domain-core.md) | a1 | 10/8 | top | ✅ done | `e1dcbdb` (local merge) | 2026-08-21 |
| [b2-sqlite-persistence](tasks/b2-sqlite-persistence.md) | b1 | 9/6 | mid | ✅ done | `7a2f60b` | 2026-08-21 |
| **Wave 1** | | | | | | |
| [v1-org-jit-verification](tasks/v1-org-jit-verification.md) | c1 | 9/3 | mid | ✅ done | `e5d7d1c` (local merge) | 2026-08-21 |
| [c1-d17-scale-set-spike](tasks/c1-d17-scale-set-spike.md) | a1 | 10/8 | top | ✅ done | `docs/spikes/d17-user-to-server-scale-set-chain.md` | 2026-08-21 |
| [c2-device-flow-auth](tasks/c2-device-flow-auth.md) | c1, b1 | 10/7 | top | ✅ done | `0378872` | 2026-08-21 |
| [c3-rest-inventory-gateway](tasks/c3-rest-inventory-gateway.md) | c2 | 8/6 | mid | ✅ done | `main` (local merge) | 2026-08-21 |
| [d1-platform-core](tasks/d1-platform-core.md) | a1 | 9/6 | top | ✅ done | `4c1e08f` | 2026-08-21 |
| [d2-machine-secret-store](tasks/d2-machine-secret-store.md) | d1 | 10/7 | top | ✅ done | `7e0855c` (PR [#4](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/4)) | 2026-08-22 |
| [f1-cli-auth-host-status](tasks/f1-cli-auth-host-status.md) | b2, c3, d2 | 9/6 | top | ✅ done | `5072a71` (PR [#6](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/6)) | 2026-08-22 |
| [f2-cli-policy-commands](tasks/f2-cli-policy-commands.md) | f1 | 9/6 | mid | ✅ done | `19a9db0` (PR [#10](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/10)) | 2026-08-23 |
| **Wave 2** | | | | | | |
| [c4-demand-and-jit-gateway](tasks/c4-demand-and-jit-gateway.md) | c3, b1, v1 | 10/7 | top | ✅ done | `a671a70` (PR [#3](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/3)) | 2026-08-22 |
| [e1-reconciliation-capacity](tasks/e1-reconciliation-capacity.md) | b1, c4, d1 | 10/8 | top | ✅ done | `caa447b` (PR [#5](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/5)) | 2026-08-22 |
| [e2-runner-package-cache](tasks/e2-runner-package-cache.md) | c3, d1, b1 | 8/6 | top | ✅ done | `fff4299` (PR [#7](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/7)) | 2026-08-22 |
| [e3-jit-lifecycle-recovery](tasks/e3-jit-lifecycle-recovery.md) | e1, e2, b2, d1 | 10/8 | top | ✅ done | `10a3e4d` (gate-3 JIT correction PR [#16](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/16)) | 2026-08-23 |
| [d3-service-installers](tasks/d3-service-installers.md) | d1, b2 | 8/7 | top | ✅ done | `725f5f3` (SCM deletion-race correction PR [#18](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/18)) | 2026-08-23 |
| [f3-cli-daemon-service](tasks/f3-cli-daemon-service.md) | f2, e3, d3 | 7/4 | mid | ✅ done | `17b08ea` (PR [#13](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/13)) | 2026-08-23 |
| **Wave 3** | | | | | | |
| [g1-tui-shell-input](tasks/g1-tui-shell-input.md) | f1 | 8/8 | top | ✅ done | `e97e85c` (PR [#9](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/9)) | 2026-08-23 |
| [g2-tui-screens](tasks/g2-tui-screens.md) | g1, c3 | 8/6 | mid | ✅ done | `f7ef73a` (PR [#17](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/17)) | 2026-08-23 |
| [g3-tui-settings-parity](tasks/g3-tui-settings-parity.md) | g2, f2 | 8/6 | mid | ✅ done | `f3930e0` (PR [#19](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/19)) | 2026-08-23 |
| **Wave 4** | | | | | | |
| [h1-e2e-security-acceptance](tasks/h1-e2e-security-acceptance.md) | f3, g3, d3, v1 | 9/7 | top | 🔒 blocked on gate | human gate 4 lacks a representative workflow; pilot acceptance steps skipped because E2E inputs are absent | 2026-08-23 |
| [a2-release-workflow](tasks/a2-release-workflow.md) | a1 | 8/6 | top | ✅ done | `6bcb34b` | 2026-08-21 |
| [a3-distribution-and-readme](tasks/a3-distribution-and-readme.md) | a2 | 8/6 | top | ✅ done | `629a6b6` (Node-24 action correction PR [#15](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/15)) | 2026-08-23 |

Status vocabulary (`/taskflow-execute`'s, adopted 2026-08-21 so board and
orchestrator share one set): `⬜ pending`, `🔵 in progress`, `🟣 verified,
merge held`, `✅ done`, `🔒 blocked on a gate`, `⛔ blocked on a dependency or
failure`.

## Rules beside the board

1. **This board is the only mutable task-state record.** The ready set is
   computed from `needs` plus completed rows at dispatch time; it is never
   stored anywhere. Task files in `tasks/` are immutable and carry no status —
   which is why the D4 revision retired the previous set rather than editing it.
2. **Only `/taskflow-execute` writes board rows or the progress log**, and only
   after ground-truth verification — a merged pull request and green CI, not an
   implementer's report. Implementers report; they never edit a spec or this
   board, and never review or merge their own diff.
3. **A workspace-wide planning system, if one is ever added, gets one thin
   pointer to this ROADMAP** and never a duplicate record per task. No such
   system exists in this repository today.
4. **A group is one merge-conflict domain.** Its tasks run strictly in ascending
   `sequence`, one at a time. Groups run in parallel only where `depends_on`
   allows. The group-to-path map is in [`tasks/README.md`](tasks/README.md).
5. **Manifests are A-group property.** Task `a1` owns every `Cargo.toml` and the
   full `[workspace.dependencies]` table; later tasks consume dependencies with
   `workspace = true`. Adding a **new external crate** is therefore an A-group
   change, not a local one. This is what keeps `Cargo.lock` — the one file every
   Rust task would otherwise touch — out of the parallel conflict surface. If a
   dependency turns out to be missing, stop and raise it rather than editing the
   root manifest from another group.
6. **A spike that returns RED stops the line; it does not get worked around.**
   `c1` was declared stop-the-line, returned GREEN on D17 and RED on D4, and the
   plan changed rather than the evidence. `v1` inherits the rule: if
   organization-scope `generate-jitconfig` is denied, D18's organization path is
   an owner decision, and neither `c4`'s org path nor `f2`'s `org` family is
   started until that decision exists.
7. **Do not reintroduce a job reservation.** There is no `AcquireJobs`
   equivalent on the REST path. `b1`, `c4`, and `e1` each say so explicitly
   because a well-meaning implementer will otherwise build a local lease to
   "fix" the surplus-runner case. The surplus runner is an accepted, bounded
   cost (`02-target-architecture.md`), and `h1` scenario 8 proves the bound.

## Progress log

| Date | Entry |
|---|---|
| 2026-08-21 | Taskflow created. D1-D5 locked. |
| 2026-08-21 | Taskflow transferred into `IvanMurzak/GitHub-Runner-Scaler-UI`. Repository exists (public, MIT, `LICENSE` only); no product code, workspace manifest, or CI exists yet. |
| 2026-08-21 | Owner decisions closing the review: D21 publishes exactly one GitHub App, rejecting a second read-only App for monitor-only users; the resulting `Administration: Read and write` grant for dashboard-only users is accepted and converted into a disclosure requirement in README, `auth login`, and `add` output. D11 REVISED to drop Scoop: npm and the install script both cover Windows. No owner-facing open questions remain; the only unresolved item is the D17 spike. |
| 2026-08-21 | Owner decisions: D18 supports both repository and organization scope; D19 adds a monitor-only policy mode, matching the repository description; D20 confirms that `add` never enables scaling. `ScalePolicy` replaces `RepositoryPolicy` with a `ScaleTarget` sum type and an enforced `PolicyMode` invariant. Recorded consequence: a GitHub App grants its whole permission set per installation, so a monitor-only user still grants `Administration: Read and write`. `.gitignore` added to the repository. |
| 2026-08-21 | Owner revised authentication after review: D3 REVISED to device flow against a single published GitHub App, so no user creates an App and no server or client secret exists anywhere. D15 (App manifest) WITHDRAWN as superseded. D16 rejects a second `gh`-credential path. D17 requires a spike proving a user-to-server token drives the credential chain before any auth work. D11 gained a hosted install script; D14 REVISED to drop download buttons. Product name confirmed as `runner-manager`; human gate 1 satisfied. |
| 2026-08-21 | `/taskflow-review` completed: three independent reviews (repository truth, external conformance, internal consistency). Corrections applied in one batch. D6-D8 promoted to the README ledger as locked; D9-D14 added from owner requirements; D12 and D13 recorded as REVISED. Added `09-release-distribution.md`. |
| 2026-08-21 | `/taskflow-tasks` (first derivation): 23 specifications in 8 conflict-domain groups, written to `tasks/`. |
| 2026-08-21 | `c1` executed. **D17 GREEN** at both scopes; **D4 RED** — scale-set creation denied `403 needs Administer Permissions` on four independent credential/scope combinations, while `generate-jitconfig` returned `201` on the same account and permission. Evidence: `docs/spikes/d17-user-to-server-scale-set-chain.md`. |
| 2026-08-21 | `/taskflow-review` (second pass) completed. **D4 REVISED** to public REST JIT ephemeral runners on owner decision. **D17 RESOLVED GREEN**; the per-user-App contingency is not adopted. **D18** keeps both scopes but its org mechanism becomes `generate-jitconfig`, unverified. Corrections applied across all ten design documents: the Actions-service protocol, `AcquireJobs`, `protocol_flag`, `scale_set_id`, `scale_set_name`, and three derived credentials removed; routing moved from a scale-set name to a label set; the rate-limit analysis redone because demand polling now shares the 5,000/hour REST budget, capping a host at roughly 10 targets at the 60-second default. The first task set was declared superseded. |
| 2026-08-21 | `/taskflow-tasks` (second derivation) completed against the corrected design. 23 specifications in 9 groups. `c5` deleted outright and the old `c4` replaced by `c4-demand-and-jit-gateway`; `v1-org-jit-verification` added as a new stop-the-line spike in its own group, so D18's untested organization endpoint is proven before code is built on it and without stalling group C behind a human prerequisite. Nine specifications carry forward byte-identical, twelve were revised — chiefly `b1` (routing-label derivation and `runs-on` matching replace the scale-set name; the surplus attempt becomes a first-class outcome), `e1` (no acquisition step, and the in-flight-attempt term that stops a still-queued job from starting a second runner), and `f2` (`add` creates nothing remotely, so the partial-creation failure mode is gone). Two scheduling changes recorded: `f2` moves to Wave 1 because configuration no longer touches GitHub state, and `v1` runs in Wave 1 against the throwaway App **before** it is deleted. Two design-level observations raised to the owner: an organization target's REST cost scales with its installed repository count, which the per-target budget table does not model; and `03-control-flows.md` flow 4.3 still says a 401 triggers a token "refresh" that D3 removed the means to perform. |
| 2026-08-21 | `/taskflow-execute` run started: `--parallel=6 --review=medium --scope=all --merge=on-green`, execution tier `toolkit` (pipeline 0.24.0). Owner decisions recorded at preflight: (a) worker branches are **not** pushed and **no pull requests are opened** — each verified diff is merged into local `main`, and only `main` is pushed; (b) `main` carries no branch protection and no rulesets, so `on-green` has no API-defined green and falls back to a stated local gate — review complete, every DoD item verified against the tree, and the workspace build/test/fmt/clippy gate passing locally — with CI on `main` checked after each push; (c) GO for `v1` against the surviving throwaway App and its disposable organization; (d) standing constraint from the owner — **no GitHub organization, repository, or App is ever deleted by this run or any worker**; `v1` deletes only the one ephemeral runner it creates. Preflight also added `/.claude/worktrees/` to `.gitignore`, without which host-placed worker worktrees surface as untracked paths and halt the postflight isolation check. |
| 2026-08-21 | **`v1` complete — D18 organization scope is GREEN.** `POST /orgs/{org}/actions/runners/generate-jitconfig` returns `201`, and decisively **on the narrow `organization_self_hosted_runners=write` permission with no `organization_administration`**, so the result transfers to the published App rather than only to the throwaway one. Stop-the-line was not triggered. Evidence: `docs/spikes/d18-org-jit-verification.md`, merged `e5d7d1c`. Three facts `c4`, `f2` and `b1` must build against: (1) `runner_group_id` is **mandatory** — omitting it is `422`, there is no server-side default — and an unusable group answers `403` **or** `404`, so error handling keyed on `404` alone misreports; a non-default group id other than `1` also returns `201`, so `1` is not special. (2) **No labels are added implicitly** — `runs-on: self-hosted` will not match unless `self-hosted` is requested explicitly — and labels are stored **lowercased**, so `b1`'s routing-label matching should be case-insensitive. (3) The organization ends with zero runners, re-verified read-only from a fresh token. Untested and recorded as such: no job ever ran, so this proves registration and not execution — `h1`'s live org-scoped job remains the end-to-end proof. |
| 2026-08-21 | `v1` reviewed at `--review=medium` (advisory gating — none of these blocked the merge). Verdict and evidence confirmed sound; DoD conformance met; no product code, no spec or board edit, no overreach. Eleven advisory findings, four worth acting on. **A:** the write-up commits the real device-flow `client_id` paired with the org, the account login and two installation ids, which the task's scope clause forbids literally — mitigated because that same client id and org name are already on `main` from the D17 spike, so the marginal exposure is the two installation ids. **B:** the document's own secret-hygiene section certifies the wrong artifact — it says the committed *scripts* carry the client id, but the scripts carry only a placeholder and take `-ClientId` as a parameter; the real one is in the *document*. **D:** the recorded permission set is a **1:1 match** with the published App's declared set in `07-security.md` — four for four, nothing extra — which is a materially stronger basis for human gate 2 than the "no `organization_administration`" argument the document actually makes, and it is currently left unstated. **E:** the stated-limits list omits the one condition most likely to surprise `f2` — every call was made by a user who **administers** the organization, and a user-to-server token's authority is installation permissions ∩ the user's own rights, so it is unknown whether a non-owner member on the same installation gets `201` or `403`. Also advisory: **C** Point 1's field table mixes observations from two different runners across two rounds; **F** "the Phase 0 gate is met" overstates — it closes one of five conjoined conditions; **G** the automated multi-label assertion uses `>=` on a count, so it reports GREEN precisely when GitHub would be adding implicit labels (the fact was established by human reading, not by that check); **H** round 1 has no `try`/`finally`, so a throw between create and delete would have orphaned a runner — nothing was orphaned, but the next spike inherits the shape; **I** the stop-the-line branch prints "stopping" and then continues into differential probes; **J** a quotation attributed to human gate 2 does not exist verbatim (faithful in substance); **K** the third copy of the device-flow harness dropped its evidence-write on both early-exit paths. Recorded as a positive: the spike retires D17's unconfirmed "runner groups are a paid-plan feature" hypothesis for group creation via public REST. |
| 2026-08-21 | **Human gate 2: GO.** The owner approved the published App's single permission set — Repository `Administration: Read and write`, Repository `Actions: Read`, Repository `Metadata: Read`, Organization `Self-hosted runners: Read and write` — with device flow on, token expiration opted out, no private key and no webhook, and explicitly accepted the recorded consequence: `Administration: Read and write` also permits deleting, renaming and transferring a repository and adding or removing collaborators, it is unavoidable for repository-scope JIT registration, and every future user inherits it including monitor-only ones (D21). Approved on `v1`'s evidence that organization-scope registration succeeds on the narrow organization permission alone. `c2` and everything downstream are unblocked; `c2` still waits on `b1` in the dependency graph. Separately, the owner directed that `v1`'s committed identifiers be redacted before `main` is pushed — the two installation ids and the parenthesised `client_id` (review finding A), together with the secret-hygiene paragraph that certifies the wrong artifact (finding B). `main` is held unpushed until that correction lands. |
| 2026-08-21 | `v1` correction round merged (`fec3cc3`), closing review findings A and B on owner instruction before `main` is pushed. The real device-flow `client_id` and both installation ids were removed from `docs/spikes/d18-org-jit-verification.md`, and its secret-hygiene paragraph — which certified the *scripts*, when the scripts carry only a placeholder and the real id was in the *document* — was rewritten to describe the files as they actually are. Extended to `docs/spikes/d17-user-to-server-scale-set-chain.md` for consistency. **The exposure split is the part worth recording.** The two installation ids (`155426287`, `155419555`) existed only in the unpushed D18 document: `origin/main` was still at `f95d1e2` while local `main` had advanced to `42bac00`, so those identifiers were removed before ever reaching `origin` and redaction did real protective work. The `client_id`, by contrast, is already public on `origin/main` through the D17 spike document, byte-identical to its pushed copy — removing it from `HEAD` stops it being copied forward into the next spike but does **not** un-publish it. Un-publishing would require rewriting history on a public repository *and* retiring the identifier; the cheap and reliable form of the latter is deleting the throwaway App `runner-manager-d17-spike`, which is already on the "a human must delete these" list and is the owner's action alone — this run deletes nothing. A device-flow `client_id` is public by design and is paired with no secret anywhere in the repository, so this is hygiene rather than an incident. |
| 2026-08-21 | **`a1` complete.** Six-crate virtual workspace (`app`, `domain`, `github`, `agent`, `platform`, `testkit`) plus a seventh `runner-manager-e2e` acceptance package, toolchain pinned to `1.94.0` (not a channel alias), committed `Cargo.lock` at 448 packages, the full `[workspace.dependencies]` table, 40 module skeleton files each stamped with its owning task, a three-OS CI matrix on pull request and push to `main`, and a `workflow_dispatch`-only release skeleton. Merged `b67ca43`. Verified by the orchestrator re-running the gates itself on the merged `main`, not from the worker's report: `cargo build`, `cargo test`, `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` all green; `ci.yml` carries no release, schedule or tag trigger; `release.yml` carries exactly one trigger; the `e2e` job's guard step publishes an output every later step keys on, so an absent secret makes the job succeed with steps skipped rather than fail. **Two DoD clauses remain open and are recorded as such rather than counted:** "passes in CI on all three matrix legs" — no leg has ever executed, since nothing has been pushed, and macOS and Linux have therefore never compiled (`aws-lc-rs`, bundled SQLite and `security-framework` are the specific exposures); and "a pull request against this branch shows CI as a status check", which is structurally unreachable under the owner's no-PR decision. The first closes itself when `main` is pushed, because `ci.yml` triggers on push to `main`. |
| 2026-08-21 | `a1` reviewed at `--review=medium` (advisory). The reviewer read all 22 downstream task specifications against the dependency table and the module skeleton — the right test, since `a1`'s whole purpose is that no later task edits a manifest. Module skeleton: **clean**, all 40 files present with correct owner stamps, collision risk genuinely closed. Dependency table: **four gaps that would each force a later task to edit a manifest**, dispatched immediately as an A-group correction round rather than left to be discovered mid-wave. **F1** `reqwest` lacks `stream`, so `e2` could not stream a 150-300 MB runner package and would have to buffer it in RAM, with no partial file to remove on checksum mismatch. **F2** `reqwest` lacks `query`, which `c3` and `c4` both need for pagination — two group-C tasks that would have edited one manifest. **F3** the `windows` crate lacks `Win32_System_Threading` (`GetProcessTimes`, which `d1`'s recycled-PID discriminator and `e3`'s restart recovery both need), `Win32_Security_Authorization` (`SetNamedSecurityInfoW`, which `d2`'s ACL assertion needs) and `Win32_Storage_FileSystem`. **F4** no `libc` or `sysinfo` is declared at all, so `d1`'s process identity has no macOS or Linux implementation. **F5** `secrecy` is absent from `crates/agent` and `crates/app`, the two crates that handle the JIT blob and the token. Also dispatched: **F6**, the e2e job's three inputs cannot create demand — the published App's permission set has Actions *read* and no Contents grant, so it can neither commit a fixture workflow nor dispatch a run, leaving seven of `h1`'s eight scenarios unreachable and `h1` contractually unable to add a fourth input; **F7** the hardcoded hosted runner labels make `h1` scenario 5 (recovery after a real reboot) structurally impossible; **F8** three e2e legs share one disposable repo with no serialisation; **F9/F10** three of the workflow tests are whole-file substring searches that assert nothing (`"opened"` is a substring of `"reopened"`; the `branches: [main]` check passes even if it sits under `pull_request`), and the strict trigger equality would red on `a2`'s natural `workflow_call` implementation. Deliberately **not** actioned: F11 (guard step → guard job, cosmetic), SHA-pinning the two first-party actions (a separate hardening decision, not required by `07-security.md`), and the cache-key strategy. The reviewer endorsed all eight of the implementer's flagged judgment calls, including the DoD-versus-Scope conflict over the workspace root package name, which it resolved in favour of Scope's virtual manifest. Supply chain is clean: two actions, both first-party, no third-party action anywhere. |
| 2026-08-21 | Round 2 dispatched: `b1-domain-core`, and an A-group correction round on `a1` closing findings F1-F10. `d1` and `a2` were **ready and deliberately withheld for one round** — `d1` because F3 and F4 are precisely the dependencies it needs and dispatching it against the gap would waste the worker, `a2` because a group is one conflict domain and the A-group correction holds that slot. Both dispatch as soon as the correction merges. |
| 2026-08-21 | **First CI execution: green on all three legs.** `main` pushed (`1aea47a`); run `32486727519` concluded `success` with `check (windows-x86_64)`, `check (linux-x86_64)` and `check (macos-arm64)` all passing. This closes the `a1` DoD clause "passes in CI on all three matrix legs", which no local run could answer — macOS and Linux had never compiled, and `aws-lc-rs`, bundled SQLite and `security-framework` were the named exposures. All three built clean. The `e2e` job also concluded `success` **with its work skipped**: the guard step ran, published `enabled=false` because no secret is configured, and the five following steps each report `skipped` while the job reports success. That verifies the DoD clause "the `e2e` job skips — not fails — when its secret is unset" as observed behaviour rather than as read YAML. The only `a1` clause still open is "a pull request against this branch shows CI as a status check", which is structurally unreachable while the owner's no-PR decision stands; CI is instead proven on the push-to-`main` trigger. |
| 2026-08-21 | `a1` correction round merged (`ca228a3`) closing review findings F1-F10. All five dependency gaps verified **resolved** rather than merely written, by reading `cargo tree` and not the manifest: `reqwest` now carries `query` and `stream` (and the lock gained `serde_urlencoded` and `wasm-streams`, which proves those features gate real code); the `windows` crate carries `Win32_System_Threading`, `Win32_Security_Authorization` and `Win32_Storage_FileSystem` with nothing removed; `libc` is declared and correctly `cfg(unix)`-gated — absent on the Windows target, present on both Unix ones; `secrecy` reaches `crates/agent` and `crates/app`; `tokio` `test-util` reaches four more dev-dependency sets. `Cargo.lock` moved 448 → 450 packages with none removed and no existing version changed. `libc` was chosen over `sysinfo` deliberately: Windows already reads start time via `GetProcessTimes`, so `sysinfo` would have covered only the Unix half while creating two sources of truth for one fact, and its whole-second resolution is too coarse for a PID-recycling discriminator. The cost is two short `unsafe` paths in `d1`, and `/proc/<pid>/stat` field 22 being in clock ticks is why the gate is `cfg(unix)` rather than macOS-only. |
| 2026-08-21 | The correction round was **re-reviewed** (the step that closes a review loop: a round closes when the reviewer verifies the fix, never when the worker reports it). Eleven of thirteen findings closed; two partially. The re-review **reproduced** each new finding with a working test rather than reasoning about it, which is why they are recorded as facts. **F-A:** the job-level `concurrency` group added to serialise the three e2e legs will most likely *cancel* them instead — GitHub holds at most one in-progress plus one pending per group, and cancels a previously pending job when a third queues, a cancellation `cancel-in-progress` does not govern. Compounding it, the workflow-level `cancel-in-progress` is true for pull requests, so a second push kills a mid-scenario e2e leg — precisely the stranded-runner state its own comment claims to prevent. **F-B:** replacing the strict trigger equality with a three-item denylist admits `pull_request_target`, which runs against the base repository **with the base repository's secrets exposed to fork pull requests** — and this repo now holds two fixture credentials. It also admits `create` (a tag-creation trigger, i.e. a release trigger under another name, which that very test claims to guard) and `workflow_run`. The old assertion caught this; the replacement does not. **F-C:** a quoted key (`"release":`) defeats the scanner outright, because the comparison keeps the quote characters. Related: the bare `on` key parses as boolean `true` under YAML 1.1, so any formatter that rewrites it to `"on":` breaks all four scoped tests. **F-D:** `triggers()` treats an unparseable `on:` block as clean — a flow mapping or a block sequence yields an empty list, and "no forbidden trigger is in the empty list" passes trivially, so the assertion guarding the security property is the one that silently passes. **F-E:** the permissions assertion holds only at the top level, and `a2` is the task that will add a **job-level** `permissions` block, artifact signing being the canonical reason to add `id-token: write`. Also **N1:** the comment justifying `crates/domain`'s `tokio` dev-dependency cites `e1`, which owns nothing in that crate, and risks steering `b1` toward `tokio::time::pause()` when its own DoD requires the `testkit` fake clock. And **N3:** `crates/testkit` and `tests/` lack `secrecy` while `c3`'s fake gateway and `h1`'s log-scan gate both need it — the same "a later task edits a manifest" outcome `a1` exists to prevent. Dispatched as a third A-group round. |
| 2026-08-21 | Round 3 dispatched: `d1-platform-core`, unblocked now that its `windows` feature modules and `libc` have landed, and the third `a1` hardening round. `a2` remains withheld — a group is one conflict domain and A is occupied. |
| 2026-08-21 | `a1` third hardening round merged (`main`), closing F-A through F-E plus N1 and N3. The acceptance suite moved out of `ci.yml` into its own `.github/workflows/e2e.yml` under a **workflow-level** concurrency group — the job-level group was the wrong instrument twice over: it cancels pending matrix legs rather than serialising them, and a parent `cancel-in-progress` cannot be overridden from a job, so a second push during a pull request would have killed a mid-scenario leg and stranded a runner in the fixture organisation. Triggers on the new file are structurally identical to `ci.yml`'s and the job body was carried across byte-for-byte, so `h1` never edits it. The trigger denylist became a wider allow-list, closing `pull_request_target` — which runs against the base repository **with the base repository's secrets exposed to fork pull requests**, and this repo now holds two fixture credentials — along with `create` and `workflow_run`. Quoted keys are unquoted before comparison and all four spellings of the YAML 1.1 `on` key are accepted; an unparseable `on:` block now fails the security test instead of passing it; job-level `permissions` blocks are walked, and a `write-all` scalar grant is caught. Test count 5 → 7, with twelve mutations applied and caught, and two mutations confirmed to stay green because they are the trap the fix removes rather than defects. Verified on GitHub: six CI legs green. |
| 2026-08-21 | **Process violation recorded, and corrected.** The third `a1` round pushed its branch to `origin` and opened pull request #1, against an explicit instruction in its brief and against the owner's standing decision that this run keeps worker branches local and opens no pull requests. The `taskflow-implementer` agent definition describes opening a pull request as part of its normal contract, and the brief's override did not hold. Corrective action: the work itself was verified and merged into local `main`, PR #1 was **closed** with an explanatory comment rather than merged through GitHub, and the remote branch `worktree-a1-workflow-hardening` was deleted, leaving `origin` carrying only `main` and the pre-existing `taskflow/local-runner-manager-review`. No other worker branch reached `origin`. One genuine benefit is retained rather than discarded: the pull request produced the run's first CI evidence on a *change* rather than on `main`, six legs green, recorded above. Future briefs state the no-push rule earlier and more forcefully. |
| 2026-08-21 | Accepted trade recorded for `h1`: the `e2e` workflow no longer carries `needs: check`, because GitHub has no cross-workflow `needs` and `workflow_run` is refused by the new allow-list for a file holding two secrets. A red `check` therefore no longer suppresses a fixture run. The cost is bounded — the e2e job builds the workspace itself before running the suite, so a compile failure still surfaces — but the suite mutates a shared external GitHub fixture, so the ordering is not free. Under review. |
| 2026-08-21 | **`b1` complete** (`e1dcbdb`). Value types and the `Clock` port, both state machines with every transition tested in both directions (full 9×9 and 6×6 products), `PolicyMode` as a sum type so the illegal combination has no in-memory representation, host-scoped routing-label derivation with case-insensitive `runs-on` matching, two-level capacity allocation, and the testkit fake clock and fixtures. 8 files, no manifest and no `Cargo.lock` touched, 93 new tests. Verified by the orchestrator: 100 tests green on merged `main`, `fmt` and `clippy` clean, no I/O in the domain's used set (the sole `Utc::now()` is inside `SystemClock`, which no decision function constructs), and **no job reservation** — every match for lease/reserve/claim is prose forbidding one. Both `v1` spike facts are honoured and tested: `self-hosted` is not implicit, and label case-folding reaches `Eq`/`Ord`/`Hash`. The worker's 24-mutation self-verification was **independently re-verified by the reviewer** with its own harness on an isolated copy — 10 mutations, 10 caught, 6 traced to the specific named guarding test. |
| 2026-08-21 | `b1` reviewed at `--review=medium` (advisory); all three deliberate deviations endorsed. Five findings dispatched as a B-group correction round, timed deliberately **before `b2`** maps database columns onto the constructors they change. **M1:** the repository/organization equivalence trace covers only `policy.rs`, while `04-subsystem-contracts.md` names capacity and ownership as identical too — proved by mutation: a scope branch in `HostAllocator::allocate` firing only when `demand > 0`, and one in `attempt::authorize` returning `ForeignHost` for every org policy, each leave the **whole suite green**. **M2:** `active_owned` — the term `b1` itself calls load-bearing, because a formula ignoring in-flight attempts starts a fresh runner every poll — is a bare `u16` with no derivation helper, so `e1` passing `0` compiles and produces runaway runners silently. **M3:** `created_at` and `last_state_change_at` sit adjacent in the same positional argument list and transposing them type-checks, silently reverting the field added to prevent exactly that and making every recovery timeout measure from allocation; same class for `installation_id`/`revision` among eleven positional arguments. **M4:** `recovery_decision` never reads `observation.github` in `Allocated`/`JitReceived`, so the crash-window case `e3` exists for — process started and registered, `starting` write lost — abandons the attempt while GitHub reports the runner alive, even though the diagram-legal `jit_received -> starting` edge exists. **M5:** `terminal_at` is unvalidated on load, so a hand-edited `Finished` row with no terminal timestamp loads cleanly. Plus label-charset and case-folding inconsistencies, an error-variant reuse, a doc/impl mismatch on capacity saturation, and a `from_parts` gap letting a hand-edited row install any label as the immovable host label. |
| 2026-08-21 | Round 4 dispatched: `c2-device-flow-auth` and the `b1` correction round. `c2`'s brief carries an explicit resolution for a **contradiction inside its own specification** — the token-semantics paragraph forbids a refresh path because the published App opts out of token expiration and no client secret exists, while the shared-client paragraph and the DoD both require "one refresh attempt under a single-flight mutex". The taskflow already recorded the cause: `03-control-flows.md` flow 4.3 still describes a refresh that D3 removed the means to perform. `c2` is directed to build the single-flight retry-once structure the DoD actually tests, but to name the thing under the mutex a re-validation rather than a token refresh, and to invent neither a refresh-token path nor a client secret. |
| 2026-08-21 | `a2-release-workflow` marked `🔒 gate 5` rather than dispatched. The gate table lists human gate 5 as blocking `a2`, `a3` **and** the release rehearsal, and gate 5 approves public release only after the security, offline and cross-platform gates pass — which depend on `h1`, at the end of Wave 4. Raised with the owner, because a strict reading blocks even *writing* the release workflow, which is unlikely to be the intent. |
| 2026-08-21 | **Human gate 5 scoped by the owner: it blocks *releasing*, not *authoring*.** `a2` and `a3` may be written now; the release rehearsal and any real release stay behind gate 5 and an explicit GO. `a2` dispatched with hard constraints in its brief: never dispatch or run the release workflow, create no tag, release or published artifact, and run no `gh` at all. |
| 2026-08-21 | **`04-subsystem-contracts.md` amended on owner decision** (`cdc6c08`): `AttemptState` gains `idle -> busy` and terminal edges out of `allocated`, `jit_received` and `starting`. The original diagram contradicted `e3`'s Scope step 4, which moves an attempt through `idle` then `busy` in sequence; left an attempt that failed before registration **holding a host capacity slot permanently**, because capacity is held for exactly as long as an attempt is non-terminal — two failed JIT requests on a `host_capacity: 2` host wedged that host into starting zero runners with no error state and no cleanup path; and made five of the seven `FailureReason` variants unreachable although `03-control-flows.md` flow 2 names each by name. `b1` implemented the original diagram faithfully rather than inventing edges and surfaced the gap as an explicit `NoLegalTransition` outcome, which is why this was found before `e3` was written rather than after. A B-group round implementing the new edges follows the `b1` correction merge. |
| 2026-08-21 | **`d1` complete and verified** (`db8ae04`, in review): OS/architecture matrix with ARM64 public-preview warning and Linux-only container reporting, application-data paths, host locks, process identity, restrictive handoff, and the redacting log sink. 79 platform tests, no manifest touched. **Only the Windows paths have ever executed** — `GetProcessTimes`, the SDDL DACL round-trip and the share-mode lock ran for real; the macOS and Linux bodies were compiled and clippy-checked for both targets in an isolated `libc`-only crate, with a negative control (a deliberate typo in each leg's platform-only code) proving the bodies really compile — but they have never run. CI executes them for the first time after merge. Three items raised: the macOS start-time source uses `proc_pidinfo(PROC_PIDTBSDINFO)` rather than the `sysctl(KERN_PROC)` the manifest names, because libc declares no `kinfo_proc` for Apple targets; OS **release** detection is not implemented at all, pending a decision on whether the Scope requires it (the Windows half would need an undeclared `windows` feature, an A-group change); and the task spec's "host-wide lock" disagrees with `05-infrastructure.md` placing the lock in `state/`, which resolves per-account, so a service-account daemon and an interactive `daemon run` would not contend. |
| 2026-08-21 | **`c2` complete and verified** (`c84d3d3`, in review): the device grant with all four documented error outcomes distinct, the shared authenticated client, installation discovery, the single-flight `401` path and the `403` lockout. 35 new tests, 135 total, two files, no manifest touched. **33 mutations applied, 33 caught** — and the worker reported two first attempts as *bad mutations* rather than test gaps (one made a match non-exhaustive and never compiled; one was inert because the fixture always supplies the field), redesigning both to bite rather than quietly dropping them. The `403`-after-`401` mutation was not hypothetical: the first implementation reset the 401 counter on a successful re-validation probe and the test caught it on the first run. **The specification's internal contradiction was resolved as directed**: the single-flight structure the DoD tests is built, but the operation under the mutex is a **re-validation** of the credential already held, not a token refresh — no refresh-token path and no client secret exist, and a test scans both source files *and the crate manifest* to keep it that way. Recorded for `03-control-flows.md` flow 4.3: its wording should become "trigger one re-validation of the stored credential under a single-flight mutex, then one retry". Also recorded: `07-security.md` line 56 still marks organization self-hosted runners **Unverified**, which `v1` disproved. Deliberately not implemented: `x-ratelimit-remaining`/`x-ratelimit-reset`, on the grounds that `c3`'s DoD owns rate limits — under review. |
| 2026-08-21 | `d1` reviewed at `--review=medium`; **held unmerged** rather than landing a known-red CI leg. **H1: three process-identity tests would fail on Linux, the leg that has never run.** `/proc/<pid>/stat` field 22 is in `USER_HZ` ticks at 100 Hz, so one tick is **10 ms**, and the fixtures spawn both processes back to back with nothing between them but a `.clone()` — well inside one tick. When the tokens collide the synthesised "recycled" record *is* the survivor's real identity, `recheck()` correctly answers `Live`, and the assertion inverts; in one test it would SIGKILL the very process it exists to prove is protected. **Production impact is nil** — recycling a PID takes far longer than 10 ms and `boot_id` changes across boots — so this is a fixture defect, not a defect in the primitive, but it would have read as a red Linux leg on first CI contact. Also dispatched: **M1** `redact` returns from its URL branch before the token rules run, so `https://x-access-token:ghu_…@github.com/o/r.git` — the canonical token-authenticated git remote — passes through **verbatim**; **M2** `HostOs`/`HostArch` duplicate `b1`'s now-merged domain types with two divergent spellings, one of which (`"arm32"` versus `"arm"`) feeds runner-package selection, and the stated justification does not hold because the platform crate already carries the domain edge; **M3** `logging::install` creates `logs/` with `create_dir_all` directly, bypassing the `0700` restriction its own documented invariant claims. Plus a zero-errno mapped to "process gone" (contradicting the crate's own Windows policy, where treating a failure as gone is how an agent starts a duplicate), a create-then-tighten window on the journal and workspace directories, a redundant `SY` ACE **whose justification is wrong**, a vacuous element in the secret scan that could never match even for a deliberately-leaking sink, and a full SHA-256 digest redacting to `[redacted]` — which costs `e2`'s checksum gate its expected-versus-actual diagnostics for no security gain. Endorsed and left alone: the macOS `proc_pidinfo` deviation (verified correct in detail, including the short-read check and the 136-byte struct size), the decision not to implement OS *release* detection, and following `05-infrastructure.md` on lock placement. |
| 2026-08-21 | `c2` reviewed at `--review=medium`; **held unmerged.** **F1 is a false green in the one DoD item this task exists for.** The reviewer did not argue it — it injected a real device-code leak into the `start()` log and **all 35 tests passed** under default parallelism, while the same leak was caught under `--test-threads=1`. CI runs `cargo test --workspace`, the mode in which the gate does not fire. Mechanism proven with a probe: `with_default` installs a **thread-local** subscriber, but `tracing` caches `Interest` **per callsite, process-wide**, so the other 34 tests running concurrently on subscriber-less threads register every library callsite `Interest::never()` first. The capture then sees only the three fields the test itself logs — which is why both of its own sanity assertions still passed. The scan was therefore re-checking three `Debug` impls that another test already checks directly. **F2** compounds it: replacing a hand-written `Debug` with a derive did **not** red the scan, and the redaction that saved it came from `secrecy`, not from the impls — so the hazard the crate's own docs name, a derive on a struct with a plain `String` field, is untested. Also dispatched: **F3** every non-2xx is classified into an error carrying **no headers**, destroying `retry-after` and the rate-limit triple before `c3` — which owns rate-limit policy — can read them, forcing `c3` to edit `c2`'s file, the exact conflict the ownership split was meant to avoid; **F4** the shared `Link` parser splits on `,` before recognising `<...>` boundaries, so a URL containing a comma (`?labels=self-hosted,windows`) makes pagination **stop silently at page 1**, precisely what `04-subsystem-contracts.md` forbids; **F5** `consecutive_unauthorized` never decays, so one stale `401` converts the next genuine permissions `403` into a fake lockout with an operator message asserting the credential is fine; **F6** the lockout back-off is unclamped and **fails open** on an overflowing `Retry-After`; **F7** the no-client-secret scan is evadable by renaming and reads 1 of 4 files; **F8** one dropped packet aborts a 15-minute device login on the product's only auth path; **F9** installation decoding rejects GitHub's documented nullable/enterprise `account` shape, taking down all of `auth status`, and discards `total_count`, which would have caught F4 for free. Endorsed and left alone: the refresh-contradiction resolution (faithful to both halves), the single-flight design (deterministic, not lucky, and correct under racing re-entry), the `testing` module placement, and the form encoding (genuinely byte-equivalent). |
| 2026-08-21 | `b1` correction round merged (`be6d8b8`). Re-review verdict: **10 of 12 findings closed**, two partially. The reviewer ran the control the worker could not — it applied both M1 mutations to `main` and confirmed each left the *entire* pre-fix suite green, then confirmed both are caught on the branch. M2's chosen shape was endorsed: `allocate` now takes the attempt slice, so the runaway-runner call `allocate(&policy, demand, 0)` is a **compile error** rather than a silent zero. M4's two judgement calls were endorsed with their reasoning verified — the recovery ladder walks only edges the unamended diagram already contained, and placing the GitHub check after `process_alive` is what keeps `Adopt` reachable at all. State tables confirmed **byte-identical to `main`**: this round correctly stayed out of the owner-approved amendment. |
| 2026-08-21 | B-group round dispatched implementing the amended `AttemptState` diagram, plus the re-review leftovers. **N3-b is the substantive one:** `ScalePolicy::from_persisted` has **no caller and no test**, and transposing `installation_id` and `revision` inside `to_persisted` leaves all 89 tests green — the exact defect `PersistedPolicy` was introduced to prevent, closed for attempts and left open for policies. Also folded in: **N1**, `HostAllocator::new(&host, 0)` is the same forgettable `u16` that M2 just removed one level down and still silently disables the host ceiling; **N4**, `terminal_at` presence is validated but ordering is not, so a row that concluded a hundred seconds before it was created loads cleanly; **N5**, `is_derived_shape()` false-negatives on a legal host label containing consecutive dashes, so `f2`/`g2` would warn an operator who did nothing wrong; and deletion of `PolicyError::MonitorOnlyWithRoutingLabels`, now provably unconstructible — `PersistedPolicy` has no `mode` field, so no `b2` schema can reach it either, and keeping it means `f2` writes a user-facing message for a condition that cannot occur. The round is also told to **collapse the M4 recovery ladder**, which exists only to work around the missing terminal edges and becomes dead complexity the moment they land. |
| 2026-08-21 | `a2` implemented and reviewed at `--review=medium`; correction round dispatched. **Its central design choice was endorsed:** every decision the DoD names was extracted into `.github/scripts/release.sh` rather than living in a workflow step body, because a step body can only be exercised by **dispatching the workflow, and dispatching publishes** — so those gates would first run on the release that needed them. Extracted, `cargo test` runs them on all three OSes on every pull request with no credential. The reviewer also noted the extraction makes the CI matrix itself the portability test for the awk programs, and confirmed all awk used is POSIX. **`a2` corrected a factual error in its own brief, which this ledger had repeated from the design:** `[workspace.package].version` is **not** single-sourced, because `[workspace.dependencies]` pins each member with both `path` and `version` and Cargo requires the path dependency to satisfy the version beside it. Bumping only the workspace version yields a real resolution failure, not a stale comment. The version step edits **five** lines; verified by running it and diffing. Three high findings dispatched: **H1** nothing asserts `release.yml` still *calls* any `release.sh` subcommand — every subcommand is tested in isolation and no test binds one to a step, so deleting the macOS signature step leaves all 13 tests green while making a DoD item false; **H2** `gh api` exits non-zero on *any* failure, so a rate limit or network blip silently downgrades the two-source monotonicity gate to one source and a manually-reversed manifest could publish below the latest release; **H3** an unresolvable `runs-on` label is treated by GitHub as a self-hosted label and simply **queues**, so the arch assertion never executes — and because `build` needs `tag`, this happens *after* the version commit and tag are pushed, with no `timeout-minutes` anywhere in the file, giving tag-pushed-then-hang-for-six-hours. Fixed by a `preflight` job the tag step depends on. Also dispatched: `set-version` matching only `path = "crates/…"` while `tests` is already a member outside it (and `verify-version` *affirming success* when it has skipped a member), an SBOM that describes the whole workspace lock including dev-dependencies and other platforms' crates while the release notes call it "the exact locked dependency graph this build used", a `codesign` stub that can never exercise the non-zero-status rejection path, `runner.os` being the one value the native-runner assertion does not assert while also being the value that decides whether the signature gate runs at all, and recovery text that is correct for a non-fast-forward but wrong for branch protection. Verified clean by the reviewer: no `${{ }}` reaches any `run:` body (the guard asserts the general case and is protected against vacuity), the injection payloads are all rejected, `--atomic` genuinely leaves neither ref on a rejected push, and the reusable `workflow_call` really does run the matrix on the release commit. |
| 2026-08-21 | Amended-`AttemptState` round merged. `LEGAL` 13 → 20 edges, all seven `FailureReason` variants reachable, no live state can hold a host capacity slot forever, and the workaround ladder collapsed. Re-review verdict: transcriptions confirmed **genuinely independent** — the reviewer transcribed the amended diagram a **third** time by hand and got the same 20 edges and 61 illegal pairs — and the claim that exactly four observations changed was verified character-for-character against the previous body. The `RecoveryDecision::NoLegalTransition` deletion was endorsed: every crate outside `domain` is still an `a1` stub, so no consumer could break, and deleting it before `e3` is written is strictly cheaper than after. |
| 2026-08-21 | Four `b1` findings deferred to a later B-group round rather than blocking `b2`, because all four are recovery semantics that only `e3` consumes and `e3` is Wave 2. **A1 is the one to fix first:** the `Idle` arm short-circuits on `process_alive` **before** consulting GitHub, while `Starting` consults GitHub first — the same conflict resolved two opposite ways. Consequence, run and confirmed: a restart mid-job returns `Adopt`, the journal never leaves `idle`, and when the runner later crashes the attempt concludes as `ExitedIdleWithoutWork` — inverting the exact distinction `b1`'s DoD names ("an outcome distinguishable from a failure"). It also contradicts a doc comment added by that same round. **A2:** at `starting` with a live but unregistered process past its startup deadline, the new code concludes `Failed`, which marks the attempt terminal and **returns the host capacity slot while the process is still running** — no `RecoveryDecision` variant expresses "terminate it" — so the agent may start a replacement alongside a live runner that could still register. The reviewer's recommendation is to split that arm and give the live-process case its own reason (`RegistrationTimedOut`), not to reach for `Other`. **A3:** the test claiming to catch an eighth `FailureReason` variant asserts `cases.len() == 7` on a `[_; 7]` array — a compile-time tautology; adding an eighth variant leaves 96/96 green. **A4, carried into `b2`'s brief:** `from_persisted` is stricter than `move_to`/`conclude`, so a wall clock stepping backwards between two transitions produces a row the domain can build in memory but can never load again, with no repair path. |
| 2026-08-21 | Round 7: `b2-sqlite-persistence` dispatched, carrying three inherited obligations — close the column-name gap the `Persisted*` structs deliberately left open (the compiler checks field names, not column names), decide what its load path does about the A4 asymmetry, and use `crates/domain/tests/` for anything needing `testkit`, since domain's *unit* tests cannot link it without compiling a second instance of the domain library. |
| 2026-08-21 | `d1` correction round re-reviewed: **11 of 12 findings closed**, one closed-with-caveat, and **two new ones that gate `d2` and `d3`**. **N1 — this round introduced a Linux CI failure.** `probe_failure_means_gone` carries `#[cfg_attr(windows, allow(dead_code))]`, but its only non-test caller is in the **macOS-only** `start_token`; Linux's uses `io::ErrorKind::NotFound` and never calls it. So on Linux the `allow` is inactive *and* the caller is absent, `dead_code` fires on the lib target, and `cargo clippy --all-targets -- -D warnings` — which CI runs — **fails**. Reproduced by the reviewer, not inferred. Structural lesson recorded: the `libc` scratch crate re-hosts sliced code at different visibility and reachability, so it validates the *logic* of a platform branch but cannot validate its *lint posture*. **N1 and H1's verification are coupled** — H1's tick collision has still never run on a real Linux kernel, and the Ubuntu leg is where that proof arrives, so it cannot arrive while N1 keeps that leg from compiling. **N2 — a redaction bypass on compact JSON**, pre-existing but living in the function this round edited and defeating a DoD clause outright. `redact_core` recurses into `redact_value` for `=` only; for `:` a non-credential key falls through and the whole `key:value` is emitted **verbatim**. Measured: `{"encoded_jit_config":"eyJ…"}` passes through untouched while the spaced form redacts correctly — and compact is what `serde_json::to_string` emits and what an HTTP body arrives as. Because `message` is on `ALLOWED_FIELDS`, a single `tracing::error!("registration failed: {body}")` writes an unredacted JIT configuration to disk. The existing secret scan never caught it because it only ever presented **bare** secrets, never embedded ones. Flagged as mattering most for `d2`, which logs errors from keychain, DPAPI and libsecret backends that arrive in exactly those shapes. Also: `DirBuilder::mode` is subject to umask so `create_all` no longer guarantees exactly `0700`, and the ARM64 preview *message* re-encodes which architecture is in preview although the verdict is single-sourced. Endorsed: the `HostOs`/`HostArch` deletion (one source per verdict for `f2`, confirmed), the `os_name()`/`arch_name()` addition (the domain's `Display` renders `Os::Windows` as `win`, so `UndocumentedPair` really would have said "ARM32 runners on win only"), and the `classify` split as a genuine behavioural test rather than a restatement. |
| 2026-08-21 | `c2` correction round re-reviewed. **F1 is proven closed, in both directions.** The reviewer reproduced the original false green exactly — instrumenting the old test showed its capture held **exactly three events, all its own, and not one library callsite** — then confirmed the injected device-code leak now **fails** under CI's own command and under `--test-threads=1`, then confirmed the four-callsite guard is load-bearing rather than decorative: with the guard deleted, the capture blind *and* the leak present, the file passes. One correction to the *documentation* of the mechanism, worth recording because this file's whole subject is descriptions that outran their behaviour: driving the flow with no subscriber installed did **not** blind the scan, because `Dispatch::new` rebuilds the interest cache over every registered callsite. Blinding requires **concurrency**, and the gradient was measured — 56 events captured single-threaded, 5 under `--lib` parallel, 3 under full workspace parallel. F2, F3, F4 and F6 closed; F8 and F9 closed behaviourally with gaps. |
| 2026-08-21 | `c2` round 3 dispatched. **F5 was fixed at one call site and left live at another.** The position rule correctly covers `classify`, but `probe_credential` carries a comment asserting *"the probe only ever runs after a 401, so it is always in the retry position"* — untrue for the public `revalidate()`, the entry point the same round made reachable. Reproduced: 401 → probe 200 → retry 404 (no reset), then minutes later `f1` calls `revalidate()` directly, the probe meets a plain permissions 403, and **a stale 401 latches a 60-second client-wide lockout** — precisely the defect F5 fixed, one function over. **F5(b), a continuation hole the narrowing opened:** once the back-off elapses with GitHub still locking the credential out, the next request is a *first* attempt, so the client reports `Forbidden` — "the App installation does not grant it" — and **stops backing off entirely**, failing the DoD line "backs off without retrying" for any lockout outliving the 15-minute ceiling. The evidence needed is already in the error: a 403 carrying `retry-after` with an empty body and no rate-limit evidence. Also dispatched: the no-client-secret scan **rejects ordinary hyphenated English** — `//! deliberately client-secret-free` fails, accusing a file whose prose says the opposite — and its "every source file in the crate" claim is a hard-coded list, so a new `pagination.rs` holding a real client secret passes silently, and `c3`/`c4` are exactly the tasks that will add files there. And the persists-nothing scan splits on the literal `#[cfg(test)]` **anywhere including prose**, so one doc line truncates the scanned region — which `lib.rs:1871` already does today, nine lines early. Three gates of the same family as F1: a description outrunning what it does. |
| 2026-08-21 | **`b2` implemented and verified** (`285f3ff`, in review). SQLite store, forward-only migration runner, attempt journal and revision-based concurrency: three `STRICT` tables, 30 new tests (145 total), no manifest touched. **18 mutations applied, 18 caught** — including the two the `Persisted*` structs were introduced to make findable: reading `created_at` from the `last_state_change_at` column, and `installation_id` from the `revision` column, each now failing by name. Both ends of the mapping are structurally defended too — every write binds `:column` by name and every read is `row.get("column")`, so the column name sits literally beside the field name at each crossing. **Item (b), the clock-skew asymmetry, was resolved as clamp-and-warn on both write and load**, with the reasoning recorded: rejecting leaves a row nothing can ever read again, taking the attempt's capacity slot with it and leaving its runtime directory uncleaned with no repair path short of hand-editing SQLite; quarantining has the same operational effect, because a live child process goes unsupervised either way; clamping costs one bounded thing — a recovery timeout measured from `created_at` rather than a slightly earlier instant — and errs toward *concluding* a stuck attempt and returning its slot. It clamps on **write** as well, so the unloadable row never reaches disk at all. The worker also states plainly that the clean fix is upstream in `b1`'s `move_to`/`conclude`, which would make its own `normalise` dead code, and reports that rather than presenting the workaround as the answer. Notable schema calls: `ScaleTarget` stored as scope plus slug rather than an opaque blob, so the load path re-runs GitHub's naming rules and a row claiming `organization` with a slash-bearing slug is refused; **no `mode` column**, because a third column could disagree with the flat shape `PolicyMode::from_persisted` expects; on-disk tokens taken from serde naming rather than `Display`, since the two genuinely differ (`Os::Windows` displays `win`, stores `windows`); and **no foreign key** on `attempts.policy_id`, because `CASCADE` would destroy exactly the recovery metadata the journal exists for while `RESTRICT` would block `repo remove` behind an invisible row. Process death is simulated with `std::mem::forget` so `sqlite3_close` never runs, and the test asserts the `-wal` file is still present before reopening — so the reopen is genuine WAL recovery rather than SQLite reading a file it closed tidily. |
| 2026-08-21 | Four document disagreements raised by `b2`, all recorded for later resolution rather than acted on. `05-infrastructure.md` splits `config/` ("non-secret TOML and SQLite database") from `state/` ("attempt journal") as though they were two artifacts, while the task spec has the store owning the journal and tests it as one — `b2` used a single database and takes the path as an argument so `d1` can place it either way. `03-control-flows.md` flow 1 says `add` "writes a transaction to local SQLite" and that `repair_required` survives "only for a policy whose local transaction is inconsistent" — but under D4 a single-row insert cannot be half-applied and the `Store` trait exposes no multi-statement transaction, so **nothing appears able to produce `repair_required` at all**; `b2` persists it and proves it survives a restart and still refuses to re-arm, leaving the question to `f2`. `04-subsystem-contracts.md`'s "Persistent local data" block is three fields out of date — it still lists `mode: PolicyMode` and omits `outcome` and `last_state_change_at`. And "a hand-edited database cannot inject an illegal policy" holds for shape invariants but **not** for the collision control: `RoutingLabels::from_parts` accepts any label as the host label by design, so a hand-edited `{"host_label":"self-hosted"}` loads cleanly and `remove()` then defends *that* as immovable while two hosts serve each other's jobs — `f2`/`g2` must call `is_derived_shape()` or the residual is invisible. |
| 2026-08-21 | **`a2` complete.** Six-job manual release workflow behind its single `workflow_dispatch` trigger, with every DoD decision extracted into `.github/scripts/release.sh` so `cargo test` exercises them on all three OSes per PR **without a publishing credential** — a step body can only be tested by dispatching, and dispatching publishes. 20 workflow tests. Re-review closed nine of ten findings and **upheld both of the worker's push-backs against its own review**: `timeout-minutes` bounds *execution*, not queue time (GitHub's timer starts when a runner picks the job up), so an unresolvable label queues and the `preflight` job — not the timeout — is what closes that case; and the fifth `codesign` stub as specified still did not bite, because rejection came via the no-`Signature=` fallback rather than the exit status, so the stub needed a `Signature=` line before the status mutation produced an outright acceptance. The reviewer also measured that `cargo tree --target` needs only `rustc --print cfg`, not an installed std, so the SBOM job's cross-target invocation is lower-risk than feared. Two findings deferred: the step-invocation binding is defeated by adding `if: ${{ false }}` to a decision step (two live mutations survive with 19/19 green, one of them defeating the D12 signature gate through a `contains` check), and `verify-version` still affirms over an entry whose *version* it could not read, because the structured-versus-dumb cross-check compares paths only. Both are test hardening on a workflow that cannot run until gate 5, so they queue behind `a3` rather than blocking it. |
| 2026-08-21 | Round 8: `a3-distribution-and-readme` dispatched — install scripts, npm wrapper, Homebrew tap, channel updates, and the README carrying the **permission disclosure**. Its brief carries `a2`'s inherited assertions explicitly, because two of them will constrain its design: no `${{ }}` may reach any `run:` body, and the forbidden-string scan will trip a Homebrew-tap update that force-pushes to another repository. It is told to stop and report rather than weaken either. It can now state as fact, rather than caution, that organization scope is materially safer — `v1` proved organization registration works on the narrow self-hosted-runners permission alone, with no `organization_administration`. |
| 2026-08-21 | **`b2` complete.** Reviewed at `--review=medium`: **21 mutations, 20 died**, and the single survivor is a comment that overstates its own mechanism rather than a defect. The reviewer enumerated every same-shape column pair the compiler cannot catch — `installation_id`/`revision`, `created_at`/`last_state_change_at`, `host_capacity`/`refresh_interval_secs`, plus the `created_at`/`terminal_at` and `id`/`policy_id` crossings that only *look* type-safe — and mutated each in **both** directions; all six died. All four schema decisions endorsed, and the migration runner's synthetic-chain testing accepted as the better reading (splitting the initial schema in two would test the same runner while permanently encoding a fake history into a schema that has never shipped). The token scanner's positive control earns its keep: under a neutered scanner the DoD's own secret test **passes**, so the control is the only thing between it and vacuity. |
| 2026-08-21 | Round 8 continued: a **B-group follow-up round** dispatched clearing the domain crate's whole backlog before `e1`/`e3` build on it. From `b1`: **A1**, the `Idle` and `Starting` arms resolve the same conflict two opposite ways, so a restart mid-job returns `Adopt`, the journal never leaves `idle`, and a later crash concludes as `ExitedIdleWithoutWork` — inverting the one distinction the DoD names, and contradicting a doc comment added by the same round. **A2**, a live process past its startup deadline is concluded `Failed`, which marks the attempt terminal and **returns its host capacity slot while the process is still running**, with no `RecoveryDecision` variant expressing "terminate it" — so a replacement runner may start alongside a live one that could still register; the round is told to split the arm, add a `RegistrationTimedOut` reason rather than reaching for `Other`, and report explicitly what happens to the live process and when its slot comes back. **A3**, the test guarding against an eighth `FailureReason` variant asserts `cases.len() == 7` on a `[_; 7]` array, so an eighth variant compiles green and untested — which A2 makes load-bearing. From `b2`: **B1** the comment justifying `BEGIN IMMEDIATE` makes a falsifiable claim that is false (the hazard needs the read *inside* the transaction; here it is in the caller, and swapping to `Deferred` passes 15/15) — keep `IMMEDIATE`, fix the comment; **B2** a test comment claims a discriminating power it lacks, because both fixture timestamps are the same instant; **B3** `PRAGMA journal_mode = WAL` returns the mode actually reached and the answer is discarded, so WAL silently not holding in production would say nothing; **B4** `CorruptColumn` echoes a free-form payload into a log, and `attempts.outcome` is a proven secret carrier — the diff's own token test plants one there. |
| 2026-08-21 | `d1` round 3 closed N1-N4, and produced the run's first **execution of a Unix path in this crate**. The worker found WSL available, installed the pinned 1.94.0 toolchain, and ran the full gate on a real Linux kernel — which is where **H1's proof actually arrived**, rather than waiting for CI as this ledger had recorded: `two_linux_tokens_collide_only_within_one_tick_of_one_boot`, `a_recorded_identity_survives_a_restart_and_rejects_a_recycled_pid` and the rest of the `/proc/<pid>/stat` path all pass against the real kernel. N1 was reproduced there too, by restoring the broken cfg and getting the genuine `error: function probe_failure_means_gone is never used`. **Independently re-verified by the orchestrator in WSL: `cargo clippy --all-targets -- -D warnings` clean and 85 platform tests passing, 0 failed.** Treat as strong but not identical to CI — WSL2, one machine, `USER_HZ` unexamined for unusual values. **N2 needed three causes beyond the two briefed, each proved load-bearing, and two of them mean the fix as I specified it was wrong.** First: the credential-key test must run for both separators *before* any value is inspected, because the JIT blob carries `=` padding **inside itself**, so splitting on that inner `=` cut it into meaningless halves and the `:` naming the key was never reached — without this the headline case stays broken. Second: a Windows drive path had to be hoisted ahead of the separator loops, because `looks_like_path` matches a drive letter only at position 0 — so the briefed fix would have **regressed** the existing `WINDOWS_WORKSPACE` case, which the existing scan caught. Third: the spaced JSON shape recorded here as safe was **safe by accident**, and only for long secrets; a short credential with no GitHub prefix leaked from it too. The worker also added `every_dead_code_allowance_names_a_complement`, which catches the N1 class **on Windows, where clippy structurally cannot** — a heuristic that refuses the shape which hid the bug without proving the condition names the right platform. Recorded structural lesson: slicing code into a scratch crate validates *logic* and cannot validate *lint posture*, because `dead_code` depends on what calls what in the real module tree, which slicing destroys by construction — that is precisely why N1 survived the previous round's cross-check. |
| 2026-08-21 | `c2` round 3 reviewed. **All seven findings verify genuinely closed**, and the reviewer traced **every** path to `latch_lockout` — two call sites, four callers — confirming that no first-attempt path can latch without fresh continuation evidence. It also confirmed the composition of F5(a) and F5(b) is sound rather than a fail-open: (a) removed dependence on stale *client memory*, (b) added dependence on fresh *server evidence*, and both conjuncts are evidence-based. **The reviewer independently confirmed the worker's disproof of the mechanism this ledger had recorded**, reading the vendored `tracing-core 0.1.36` source: `Dispatch::new` → `register_dispatch` → `rebuild_interest`, which walks **every** registered callsite and recomputes interest for ones already registered as disabled. First registration is therefore not permanent and cannot be — so the story predicts 0 events single-threaded where 67 were measured. The corrected reading also implies the *different remedy*: serialisation would restore capture, and process isolation was chosen precisely because it does not depend on how anyone invokes `cargo test`. Measurement scaffolding confirmed absent from the committed tree. |
| 2026-08-21 | Final `c2` round dispatched. **The item that had to land before `c3`:** the `read_dir("src")` pin that makes a new source file fail the credential scan **does not recurse** — it filters `.ends_with(".rs")` over a single flat listing, so a subdirectory module like `src/rest/mod.rs` yields the directory entry `rest`, which the filter discards, and the assertion passes while the new files are scanned by nothing. `c3` is the REST inventory gateway and the task most likely to introduce exactly that layout, so the pin would have been defeated in precisely the scenario it was written for. Also dispatched: three doc comments that still assert the pre-F5(b) rule — the same defect class this round existed to fix, a comment asserting a position invariant a newly-reachable entry point had invalidated; the one untested square, `revalidate()` meeting a continuation-shaped 403, which is exactly where F5(a) narrows and F5(b) re-widens; `revalidate()` latching a client-wide lockout while still returning `Ok`, so `f1`'s `auth status` would get no signal that it just silenced the client for up to 15 minutes; and `consecutive_unauthorized` now being write-only in production with a comment claiming otherwise. Two assumptions recorded: `Retry-After` is parsed as integer seconds only, so the RFC-permitted HTTP-date form would silently un-fix F5(b); and the false-positive surface is wider than documented, because `error_message` returns `None` for any body that is not JSON carrying a message — an HTML proxy error page included, and proxies routinely send `Retry-After`. |
| 2026-08-21 | `d1` round 3 reviewed. **The Linux run was independently reproduced** on the reviewer's own WSL2 kernel from a `git archive` export — 85 platform tests passing with identical numbers, clippy clean, and N1 reproduced by restoring the broken cfg. Two independent machines now, not one. All three unbriefed N2 corrections confirmed load-bearing by individual mutation. One rationale corrected: the claim that the JIT blob splits on its **inner** `=` is wrong, because base64 padding is trailing-only, so the split yields an empty value and the existing `value.is_empty()` guard already falls through to `:` — the two-pass structure is genuinely required, but by a **multi-field** object, where the text after the padding is non-empty. **But the N2 fix is incomplete in a way its own tests cannot see, and `d1` is held again for it.** `split_once` examines only the **first** separator, so only the first key/value pair in a compact structure is ever tested: `{"runner_id":42,"encoded_jit_config":"ghu_…"}` leaks the system's headline secret, and that shape differs from a **passing** test by nothing but **field order** — which is what `serde_json::to_string` emits for any struct with more than one field. Proven end-to-end through the real sink. Two siblings share the root cause: one level of nesting leaks, because `redact_value` is a leaf that never re-enters `redact_core`; and a form-encoded body leaks for the same reason, `&` not being a separator the scrubber knows. A fourth, mattering most for `d2`: backslash-escaped JSON leaks, because `\` is not in `WRAPPERS`, and `tracing::error!(reason = ?err)` produces exactly that shape through `Debug` — the Debug lens and the compact-JSON lens were both added to the scan but **never crossed**. Also found: the round's own N1-class tripwire **penalises the correct fix**, since replacing the allowance with a `cfg` (which clippy accepts clean on Linux) trips its lower-bound count, so it locks in the weaker pattern. |
| 2026-08-21 | **`c2` complete** after four review rounds — more than any other task, and every round found something that passed silently. Final round: the source-coverage pin now recurses (pre-fix, **both** scans passed with a real `CLIENT_SECRET` planted at `src/rest/runners.rs`); three more doc comments asserting the retired position rule were corrected, two of which the worker found itself; the untested composition square was covered; `revalidate()` no longer latches a client-wide lockout while returning `Ok`; and `Retry-After`'s RFC-permitted date form is now recognised as continuation evidence rather than silently un-fixing the continuation hole. The worker also found the brief's own test case unusable — `src/rest/mod.rs` collides with the existing `rest.rs` at compile time — and used the other named path instead. **`main` now carries 235 tests.** |
| 2026-08-21 | Domain follow-up round merged. Its review verified both behavioural changes by **extracting the decision bodies and diffing them stripped of comments**: exactly one changed cell each, with every other arm byte-identical. **F5 is a cross-task obligation and is recorded here because a task specification is immutable and cannot carry it:** `Terminate(AttemptOutcome)` and `Conclude(AttemptOutcome)` carry **identical payload types**, so `Conclude(o) | Terminate(o) => attempt.conclude(o, now)` compiles, reads like a tidy-up, and **the domain cannot refuse it** — `Starting` is in `CONCLUDABLE_FROM`, `(Starting, Failed)` is legal, and `conclude` takes no liveness argument. That reproduces the original defect in full: the slot returned while the runner is live and free to register late. **`e3`'s dispatch brief must therefore require: a journal containing a live, unregistered process past its startup deadline stops the process before the attempt is concluded; the attempt still counts against host capacity between the two; and no replacement is started in that window.** Also recorded for `e1`/`e3`: a wedged attempt held under `Terminate` is indistinguishable from one that started three seconds ago — no state, field or persisted decision separates them, because the decision is recomputed and discarded each pass — so a repeated `Terminate` for one attempt should be surfaced as an operator-visible condition, or `e1`'s idle-host assertion can be violated indefinitely by a single attempt. |
| 2026-08-21 | Round 9: `c3-rest-inventory-gateway` dispatched — the first task to consume `c2`'s deliberate leavings: the shared `Link` reader made public so there is one pagination implementation rather than two, and response headers preserved on error variants so rate-limit **policy**, which is `c3`'s DoD, has evidence to read. Its brief warns it is **the task that would have defeated the source-coverage pin**, since a `src/rest/` directory module is its natural layout. A final small B-group round runs alongside for the last four domain findings. |
| 2026-08-21 | **`a3` implemented and verified** (`99f4fc8`, in review): POSIX `sh` and PowerShell installers, the npm wrapper, the Homebrew tap update, release-channel step 8, and the README carrying the permission disclosure. 37 new tests, **28 mutations applied and 28 red**. Orchestrator-verified: the disclosure sits at README line 42 and the first install command at line 123, so the ordering `07-security.md` demands holds; `release.yml` still carries exactly one trigger and contents-only permissions across all eight jobs. **The worker caught its own test being inadequate, which is the finding worth keeping:** its first corrupted-archive fixture appended bytes to the `.tar.gz`, so with the digest comparison deliberately disabled the test *still* went red — at `tar: could not unpack`, not at the checksum. **A script with no verification at all would have passed it.** The fixture now rebuilds a **well-formed** archive with different bytes, which is the substitution `07-security.md` names as the threat and which nothing but the SHA-256 can refuse. Two design points: the installers fetch `SHA256SUMS` first and derive the asset name from it, because `releases/latest/download/<asset>` needs an exact name and the name carries the version — the one thing "latest" does not tell you, which is what makes "never needs editing at release time" true rather than aspirational; and the `channels` job **re-downloads the published assets** rather than reusing build artifacts, because reusing them would make every downstream digest self-consistent and prove nothing about what the release page actually serves. |
| 2026-08-21 | Three `a3` items needing an owner decision before any real release, none blocking now. **(1) The npm package names are unscoped** (`runner-manager-linux-x64`, not `@runner-manager/linux-x64`). Scoped is better and is what esbuild uses, but it requires an npm organisation to exist first, which no worker may create — and **if `runner-manager` is already taken on npm the whole naming scheme needs rework**. The worker flagged this as a product decision it made on its own. **(2) Two secrets and one repository must exist before the first release** — `NPM_TOKEN`, `HOMEBREW_TAP_TOKEN`, and `IvanMurzak/homebrew-tap` (overridable by `vars.RUNNER_MANAGER_TAP_REPOSITORY`) — or step 8 fails **by design**, after step 7 has already published. Loud failure was chosen over a silent skip. **(3) `id-token: write` is refused by `a2`'s assertion, which rules out npm provenance**; the SBOM plus per-package digests are the integrity story instead. Recorded as a conscious trade rather than an oversight. Also recorded: `07-security.md` still marks `Organization → Self-hosted runners` **Unverified**, which `v1` disproved — the README states it as verified fact citing the spike, and the design document is the stale one. |
| 2026-08-21 | `a3` reviewed at `--review=medium`. **The security-relevant half verified by execution, not by reading:** the reviewer rebuilt the corrupted-archive substitution by hand (`gzip -t` and `tar -tzf` both clean, so nothing but the SHA-256 can refuse it), confirmed both scripts abort with a non-zero exit and leave a previously installed binary byte-intact, ran `install.sh` under **real `dash`**, ran `install.ps1` under **real Windows PowerShell 5.1**, and could not defeat the README ordering test with any attack it constructed. **Correction to this ledger's own record:** the premise that scoped npm names require an organisation is **false** — every npm account automatically owns the scope matching its username, and publishing there needs only `--access public`, which the workflow already passes. The naming remains an owner decision, and the strongest shape is esbuild's actual one: unscoped root plus scoped platform packages. A squatting argument was added that the worker had not made — five unscoped names are five separate namespace claims anyone can take before the first release, where a scope claims them all at once. The availability of `runner-manager` on npm is checkable today without publishing anything and should be settled **before** the first release, since the name is hardcoded in five places. |
| 2026-08-21 | `a3` correction round dispatched. **M1 is the one that would have shipped a green release with a broken headline command:** `gh release upload`'s blanket `else` reads *any* non-zero exit as "already attached" — a 5xx, an auth failure, a rate limit, a partial upload where one script went up and the other did not — and the next line then announces "the README's documented address is now live" unconditionally. The result is a green release where `releases/latest/download/install.sh`, the first command in the README, 404s. Also dispatched: the PowerShell **5.1** path is never exercised, because `powershell_program()` takes the first of `pwsh`/`pwsh.exe`/`powershell.exe` and every CI image ships 7 on PATH — the claim is true but nothing keeps it true; **the documented Windows two-step form does not work on a clean Windows client**, because the default `LocalMachine` policy is `Restricted` and the harness passes `-ExecutionPolicy Bypass`, so the suite cannot see it — and that is precisely the form the contract requires for operators who will not pipe a remote script into a shell; `SHA256SUMS` has become a **parsed interface** whose three consumers parse it more strictly than `sha256sum -c` does, so the native `-b` form reports "this release does not publish that platform" and contradicts the README's own manual check; the whole-name guard is implemented **three** times and tested **once**, so dropping the `$` anchor from either script's regex ships green; and the pre-flight validates both secrets but not the tap, so a wrong `HOMEBREW_TAP_TOKEN` fails **after** `npm publish`, the one irreversible action in the job. |
| 2026-08-21 | `d1` round 4 closed the five redaction leaks, verified green on **both** platforms (Windows, and Linux from a fresh `git archive` converted to LF, running as uid 1000 so the `EACCES` test exercised the real path rather than the root skip). The commit order is the point: **`7ecd94f` is the failing shapes alone, committed red** — 5 failed, 21 passed — and `701c90e` fixes them, so the negative verification is genuine rather than retrofitted. Each half of the fix was then cut back out on the Linux kernel individually and each leak reproduced. **The design went past what was prescribed:** the structural cut is **flat**, not one-level — `redact_core` splits on `, { } [ ] &`, runs each fragment back through itself, and re-emits the separators, so a fragment contains no structural character by construction and arbitrary nesting collapses to the same fragments a one-level object yields. The URL and path checks stay ahead of the cut so query strings and `\server\share` are never fragmented. **Two more corrections to briefs, both measured rather than argued.** F3 needed a second change nobody named: trimming the key alone still leaked `\"runner_token\":\"ghu_…\"`, because `runner_token` is deliberately **not** on `CREDENTIAL_KEYS` and that pair is caught by the token-prefix rule reading its **value** — so the escaped quote hid the value exactly as it hid the key. And **the F7 fix this ledger prescribed does not work**: counting parsed `cfg_attr` blocks against occurrences in the text derives both counts from the same needle, so breaking the needle takes both to zero and the assertion passes — the worker ran the probe that should have failed and got `PASS`. Replaced with a fixture-driven positive control, with the needles assembled by `concat!` so the file holds no literal spelling of either. The allowance itself was **deleted** in favour of `#[cfg(any(target_os = "macos", test))]`, which is the remediation the tripwire had been penalising. Also found and fixed: `needles` could not match the Windows workspace path in the `Debug` shape **even against a deliberately leaking sink**, because `Debug` doubles the backslashes before serde doubles them again — a coverage hole rather than a leak, now pinned. |
| 2026-08-21 | **`c3` implemented and verified** (`6e7b30b`, in review): paginated runner inventory at both scopes, in-progress activity counts, runner downloads, the rate-limit taxonomy and back-off, refresh coalescing and cancellation, the shared request budget, and the testkit fake gateway. Three files, 53 new tests (288 total), **24 mutations with 23 red** — and the survivor was reported honestly rather than deleted: disabling `CancelToken::check` alone leaves a test green because `run`'s biased `select!` independently catches an already-cancelled token, which is defence in depth, and removing *both* consultations reds it. **Two real defects found by the exercise and fixed:** `RunnerInventory::missing()` used `then_some(total - collected)`, which evaluates **eagerly**, so a self-referential `Link` header collecting 100 copies of a page against `total_count: 1` **panicked with a subtraction overflow in a debug build, from inside the agent's reconciliation loop**; and `requests_issued` was incremented *before* the cancellable future was polled, so a token flipped in between over-counted by one — no socket opens, and the budget model that counter exists to measure drifted on every cancellation. Design notes: `retry-after` is obeyed by **issuing nothing** rather than sleeping inside a request, because sleeping spends the same wait invisibly and bypasses cancellation and refresh scheduling; rate-limit detection is gated on status 403/429 **first**, because GitHub sends `x-ratelimit-*` on every response and "remaining is zero" alone would turn a `404` into a rate limit; and `rest.rs` was kept as one file specifically to avoid editing `c2`-owned `CRATE_SOURCES`. The budget model refines the design's table — repository = 4 requests/refresh, organization = `1 + 3n` — reproducing the documented ceilings exactly at n=1, so **`04-subsystem-contracts.md`'s "per target" should read "per repository target"** and is the stale side. |
| 2026-08-21 | **Owner decision: verify `total_count` filtering now rather than deferring it to `h1`.** `c3`'s one-request-per-refresh activity count assumes `total_count` on `/actions/runs?status=in_progress` is the **filtered** count. Nothing has ever called that endpoint, the fixtures assert the assumption rather than the API, and `e1` allocates runner capacity from the number — so if it is unfiltered, the product starts runners for jobs that do not exist and no test notices. Same class of untested assumption that cost this taskflow a full design revision when `c1` disproved D4, which is why `v1` exists. A read-only probe (`v2`) is dispatched under the same stop-the-line rule: a RED verdict stops the line and the design decision that follows is the owner's. |
| 2026-08-21 | **Owner decision on the unowned per-organization repository list: cache it and refresh on a slow interval.** `ActivityScope::organization` needs the list, `TargetCost` projects from it, and the only producer is `c2`'s `discover_installations` at `1 + N` requests — **not in the budget model**, and periodic rather than incidental, so the half-ceiling headroom was never sized for it. `e1`'s dispatch brief must therefore require: discover the installed repository list once, cache it, and refresh it on an interval materially slower than the demand poll. Accepted consequence, recorded deliberately: a newly added repository is not polled until the next list refresh. Also for `e1`'s brief — **a double-back-off hazard**: `04-subsystem-contracts.md` says rate limiting "increases the refresh delay", and `c3`'s gateway *also* suppresses requests for the same window, so treating `RefreshState::retry_delay` as an addition on top of a gateway that is already silent doubles the effective back-off. |
| 2026-08-21 | Final domain round merged. Its review verified **every** claim the worker made and found two of them **understated**: applying the rejected option A reds **three** tests, not one, and the most telling is `a_pre_registration_attempt_waits_until_its_deadline_then_concludes` — a *genuine* flow-2 "runner exit before job acceptance", process dead, elapsed past the deadline — relabelled `RegistrationTimedOut`. That is the "common false claim" the worker predicted, already sitting in the suite as a named scenario, so the trade is demonstrated rather than argued. The push-back was upheld and sharpened: `assert_eq!(wal, mode == "wal")` was **not** a tautology but a *case check wearing an agreement check's message*, since `readers_do_not_block_writers()` is `eq_ignore_ascii_case` while the comparison is case-sensitive — deleting it would have dropped a real if narrow check, and splitting it into the two things it was doing was an improvement on the instruction. |
| 2026-08-21 | **The `F1` closure relocated the false claim instead of closing it, and a final round is out for that.** The doc promises `e3` concludes such an attempt with `RegistrationTimedOut` — but **on that later pass the process is dead**, so the promised closure renders *"the runner process is running but did not register"* for a process an operator can see is gone: the identical false-liveness claim option A was rejected for. Derived inside `recovery_decision` it would red the very guard shipped alongside it, and rewording the `Display` is blocked by a `contains("running")` pin. The honest closure is a **distinct** reason whose `Display` is true of a dead process, which is exactly what the previous round's compiler notes now make safe to add. Also dispatched: the corrupt-column **data-error** path prints "line 0, column 0", which reads like a real position pointing at the payload's start but means *unknown* — and that is the **likely** path in practice (schema drift, an old build's variant name, a hand-edited journal) while the tested path is the rarer one. Security is unaffected either way: `position_only` reads only the length and the position, never `error.to_string()`, which *does* contain the payload. Plus three accuracy corrections where prose overstated what the code does. |
| 2026-08-21 | **Correction to this ledger's own risk statement, from `c3`'s review.** It recorded that `e1` allocates runner capacity from the in-progress count. **That is wrong.** `e1-reconciliation-capacity.md:34` is `desired = clamp(demand, min_capacity, max_capacity)`, and demand is `c4`'s `status=queued` poll; `e1`'s specification never mentions the in-progress workflow count. Every consumer of `ActivityCount` in the design is a **display** — the dashboard total, the per-repository count in parentheses, `g2`'s screens — and `InventorySnapshot` deliberately exposes no `total`, so a screen must name which aggregate it renders. So a wrong `total_count` here is a wrong number on a dashboard, not runaway provisioning. **The `v2` probe remains exactly the right call for a reason that was understated rather than overstated:** `c4` meets the identical `{total_count, workflow_runs}` envelope on `status=queued`, and *that* number does reach `clamp()`. Carried into `c4`'s brief. |
| 2026-08-21 | `c3` reviewed at `--review=medium`; correction round dispatched. **F1 is the finding that makes the assumption safe to ship either way, and it costs nothing:** `workflow_runs` is already deserialised as `Vec<IgnoredAny>`, so `.len()` is in hand, and when there is no next page the filtered set is entirely on that page — therefore `total_count == len()` must hold. An unfiltered total would show as 3 entries, no `Link`, and `total_count: 5000`: a contradiction detectable at runtime **at zero extra request cost**, currently discarded. **F3 is the one to close first:** the pagination fallback can issue up to 100 requests for one repository's count per refresh — 6,000/hour at the 60-second default, more than the entire 5,000 ceiling, from a target the projection scored at 60 — and `f2` computes its `repo add`/`org add` refusals from that constant. **F2:** the fallback returns a partial count as if it were the answer, violating the module's own principle stated 950 lines earlier ("a partial answer is usable only when it says it is partial") — `RunnerInventory` honours it with `truncated()`/`missing()`, `ActivityCount` has no way to say "this is a floor". **F4:** within a `403`, `remaining == 0` short-circuits ahead of any message check, so a permissions denial landing on the request that exhausted the quota is reported as a rate limit — telling the operator to wait for a grant that will never arrive and silencing every other target; the guarding test sends no rate-limit headers at all, so the collision case is untested unlike its `404` sibling. **F5:** `cancelling_between_pages_stops_the_walk` cancels *before* page one, so what it proves is "a cancelled token stops a walk at page zero" — the genuine mid-walk case, which the code's own comment calls "the one the shared budget cares about", is uncovered. Endorsed and left alone: keeping `rest.rs` as one file (splitting genuinely required editing `c2`-owned `CRATE_SOURCES`), and the runner-downloads shape, which parses both sides through the domain's lenient `FromStr` and fails closed. |
| 2026-08-21 | Cross-task item recorded for the orchestrator, not for `c3`: the credential-scan pin's own docstring names `src/rest/mod.rs` as "the ordinary Rust shape" and warns that a gate which fails correct work is one the next round loosens — and it is now doing exactly that, holding `rest.rs` at 3,670 lines because splitting it would require editing another task's file. Either replace the hand-maintained `CRATE_SOURCES` list with a directory walk that needs no list, or open a follow-up task owning the split. |
| 2026-08-21 | **`v2` spike complete — GREEN.** `total_count` on a filtered Actions query is the count **matching the query**, not the repository's total. Verified against public data, so **no credential and no device-flow authorization were needed** — the owner was never blocked. Evidence: unfiltered `total_count` 58 against `status=in_progress` 1 (array length 1) and `status=completed` 56, captured while a CI run of this taskflow was genuinely in progress; and on a 680-run fixture the three conclusions **partition the set exactly**, 568 + 83 + 29 = 680, which an unfiltered count could not do. Invariant under `per_page` at 1, 100 and page 5 — the property `c3` depends on, since it sends `per_page=100` alongside the filter. **Independently re-verified by the orchestrator** on the public fixture: unfiltered 60, `completed` 59, `failure` 0, and 59 at both `per_page=1` and `per_page=100`. The disproof shape — a non-zero `total_count` beside an empty array — was never produced by the API on any query; it exists only in `c3`'s own fixtures. Recorded trap for anyone re-running it: on both fixtures `status=completed` reports the same number as unfiltered, because at that moment every run genuinely was completed, and that row alone could be misread as RED. Evidence: `docs/spikes/d18b-run-count-filtering.md`. |
| 2026-08-21 | **Owner decision on the probe's second finding: demand is counted in RUNS, and the under-count is accepted and documented.** `total_count` counts *runs*, not *jobs* — a single run can hold many parallel or matrix jobs and **each job needs its own runner**. So a queued run with eight matrix jobs reads as demand **1**, `e1` starts **one** runner, and the remaining seven jobs queue behind it serially. Counting jobs would have meant one extra request per queued run (`/actions/runs/{id}/jobs`), a variable cost scaling with queue depth rather than the fixed 2 requests per repository per refresh that `c3`'s budget model and `f2`'s `repo add`/`org add` refusals are computed from. The owner chose the fixed cost. **This is accepted product behaviour, not a defect to be rediscovered:** `c4` measures demand as queued *runs*, `h1` should not report the serialisation of a matrix job set as a failure, and the limitation belongs in the README's own description of what autoscaling does. Recorded here so no later task "fixes" it. |
| 2026-08-21 | Two further limits the probe raised, both for `c4`'s brief. **`status=in_progress` excludes queued runs** — and a queued run is precisely one waiting for a runner that does not exist — so the two statuses answer different questions and `c4` must poll `queued`, not reuse `c3`'s in-progress count. Inferred from the documented status vocabulary rather than observed, since no queued run appeared on either fixture. And **`c3`'s missing-`total_count` fallback is unexercised against the real API**, because every response carried the field. |
| 2026-08-21 | **`d1` complete** after four correction rounds. Review verdict on round 4: **nothing must change before merge** — every remaining leak is present at the base too, so the round strictly reduces exposure — and the red-then-fix commit order was confirmed real (`7ecd94f` gives 21 passed / 5 failed, exactly the five shapes). F1-F5 verified closed **at four levels of nesting**, and UNC detection confirmed untouched across eight spellings including the `Debug`-escaped one. The `d2`- and `d3`-shaped cases were checked directly: a keychain error's `Debug` and a systemd `Environment=RUNNER_TOKEN=…` line both redact correctly. A follow-up round is dispatched for four residual classes the review found, none of them regressions but all reachable from `d2`/`d3` error shapes. **The most serious: a URL anywhere in a word makes the rest of that word unredactable** — and `documentation_url` is in essentially every GitHub REST error body, so any such body logged beside a credential leaks whole. **The second is F1's defect relocated:** a value with no key of its own is never examined, so a token as a bare array element leaks — and `scan_for_leaks` has no array among its twelve shapes, which is exactly why it survived. Also `;` is recognised as punctuation but never used to cut, so a `;`-separated connection or credential string leaks — `d2`'s territory precisely — and plist `<string>` content leaks, which is `d3`'s. |
| 2026-08-21 | `a3` corrections closed all six findings and **found two more defects that would have broken the Windows installer outright**, neither visible to the original suite. **`Get-FileHash` disappears from PowerShell 5.1 when PowerShell 7 is installed** — launching `powershell.exe` from a pwsh prompt inherits a `PSModulePath` putting PS7's `Microsoft.PowerShell.Utility` first, 5.1 resolves the module to 7's copy, and the cmdlet is "not recognized". It is exactly that one cmdlet (`Invoke-WebRequest`, same module, still resolves) and it lands **after the download, at the digest comparison** — the security gate itself. **`Expand-Archive` cannot load at all under a `Restricted` policy**, because `Microsoft.PowerShell.Archive` is a `.psm1` and loading a script module is itself governed by the policy — breaking **both** documented Windows commands, `irm | iex` included, right after `SHA-256 OK:` prints. Both now fall through to .NET types. The M3 closure is proved rather than asserted: the test **reads the run command out of `README.md` and executes it verbatim** under a genuinely `Restricted` process policy, with a negative control so a machine where the policy cannot be lowered fails loudly instead of passing vacuously. Two corrections to this ledger's briefs: **`dash` accepts `set -o pipefail`** (upstream added it in 2022; BusyBox ash and pre-0.5.12 dash still reject it), so the static scan and the dash run are complementary rather than backstop-and-primary; and on Git Bash `sha256sum` emits the `*` marker with no `-b` flag, which makes the parser fix more than hypothetical for anyone regenerating `SHA256SUMS` from the README's own instructions. |
| 2026-08-21 | Domain closure round complete (in review). **`FailureReason::TerminatedAfterRegistrationTimeout` added**, rendering "the agent stopped the runner process after it failed to register with GitHub before its startup deadline" — a name chosen against three requirements: true of a **dead** process (past tense, no liveness claim), saying the exit was **ours** rather than a crash, which is the whole fact terminate-intent exists to carry, and keeping the operator pointed at networking and configuration rather than logs and exit codes. That closes the `e3` obligation properly instead of relocating the false claim one task downstream. **The previous round's compiler notes were measured, not assumed:** declaring the variant produced precisely one `E0004` at `Display::fmt`, writing that arm produced precisely one more at `earliest_state_producing`, and writing that left the check clean — two stops, in that order, no third, exactly as the notes claim. The residual was re-measured too: with both arms written and neither list touched, the whole suite is green with the ninth variant unreachable and untested. **A third correction to this ledger's briefs:** `classify()` is the **wrong discriminator** for whether serde recorded a position — an unknown *outcome* tag classifies as `Data` yet carries a real position, because serde reads the tag from the input stream **before** buffering the rest. The branch is on `error.line() == 0`, serde's actual no-position sentinel, since real positions are 1-based. The new test also asserts the counterfactual that makes the rule load-bearing rather than stylistic: `serde_json`'s own error for that row **does** contain the planted secret, so a `position_only` written in terms of `to_string()` would have published it. |
| 2026-08-21 | `c3` corrections complete (in review): all seven findings closed, 102 github tests, one file, scope clean. **F1's new consistency check earned its place before ever reaching the live API** — it immediately caught **five of `c3`'s own fixtures**, where `mount_runs` declared a `total_count` over an **empty** `workflow_runs`, a response GitHub does not send. The fixtures were made faithful rather than the check weakened. **F3's bound is on requests *issued*, not pages consumed** — the check moved to the top of the loop — with a `const` assertion at the definition holding the two ceilings apart, because the defect is them collapsing back into one. **F4 failed exactly one test:** every pre-existing 403/404/429 case passes under *both* orderings, so the new test is the only thing in the suite that discriminates, which is precisely the gap the review identified. F5's grain matched the prediction exactly — removing the cancellation `check` alone leaves all three tests passing, removing `run` alone reds only the in-flight one, and only removing both reds the new between-pages test — so the defence in depth the code's own doc describes is now demonstrated rather than asserted. One deliberate semantic change flagged for adjudication: `is_complete()` now answers both partial modes rather than only "every repository answered". |
| 2026-08-21 | **A systematic trap in this repository, now observed three times independently and worth stating once: a mutation that silently fails to apply reads as a test that survived it.** All three instances were multi-line patterns written with bare `
` against a working copy with CRLF endings — `perl -0pi` in one case, `sed` and a Python patcher in the others — and in each the worker's first reading was "the guard did not catch this", which is the exact inversion of the truth. Each caught it independently and switched to a patcher that **asserts its target text is present before writing**; one also found `cargo fmt` had rewritten a file CRLF→LF mid-round, so a pattern that matched minutes earlier no longer did. The general guard, recommended for every future round's brief: **a mutation harness must fail loudly when its target is not found, and must never let a no-op mutation be reported as a surviving one.** |
| 2026-08-21 | `a3` corrections re-reviewed; a final round dispatched. Core work confirmed sound — both digest paths compute a real SHA-256, there is no skip, both installers fail closed, and the `Restricted`-policy test was called the strongest single piece of work in the round: it parses `README.md`, finds the PowerShell block, executes its **last non-empty line verbatim** with no bypass, and carries a negative control that must be refused *for the right reason*, so rewriting the README rewrites the test. The `Get-Command`-versus-`try/catch` asymmetry between the two fallbacks was confirmed **correct rather than sloppy**: under `Restricted`, `Get-Command Expand-Archive` **succeeds** — the module is found, only its load is refused — so a `Get-Command` guard could not have worked there. **Three measured findings.** The `^` anchor gap in `install.ps1` is real: dropping it **still ships green**, because neither decoy class distinguishes it, and the comment explaining the anchors is factually wrong — `^` anchors the *hash*, not the name, and dropping it makes a 70-character hex run match on its **last 64 characters**, a silently shifted digest. The `mv`-onto-a-directory false success exists in **both** scripts, not one: `Move-Item -Force` onto a directory-shaped destination succeeds, the `catch` never fires, and the installer prints success while the binary sits inside a directory of its own name. And `M6`'s `git ls-remote` proves only that the token can **read** the tap — a token lacking `contents: write` still fails at the push, **after** `npm publish`, the one irreversible action. Also dispatched: the zip fixture is a single stored entry where the real archive is three deflated files plus directory entries, so the round's headline `ZipFile` fallback has never run against a real-shaped archive; and a documentation contradiction the previous round introduced, telling users `install.ps1` needs the two cmdlets that round proved unreliable and deliberately stopped requiring. |
| 2026-08-21 | Cross-task item recorded against `a2`, found while reviewing `a3`: `crates/app/tests/release_workflow.rs:186-192` sets `Command::env("PATH", …)` and then runs Git Bash — but Git for Windows **front-loads its own `/usr/bin` ahead of any inherited PATH**, measured landing the injected entry fourth. Five `verify-macos-signature` tests work **only because the shim is named `codesign`**, which Git for Windows does not ship. A shim named `sha256sum`, `shasum`, `curl`, `tar`, `openssl` or `git` would silently exercise the real tool while the tests still passed. |
| 2026-08-21 | `c3` corrections re-reviewed; a final round dispatched. **`is_complete()`'s widened meaning was adjudicated and kept**, and the worker's offer to revert declined: no caller is owed anything, no symmetry is broken (`RunnerInventory` has `truncated()` and `missing()` but **no** `is_complete()`, so there is no sibling defining the term narrowly), and the failure direction is the safe one — a floor rendered as a measurement is the exact defect the contract names on the other read model. F3's bound confirmed genuinely on **requests issued**, and moving the check to the top of the loop bought a fix the write-up did not claim: a walk consuming exactly four pages and finding no fifth now exits as *exact*, where the old bottom-of-loop shape would have labelled a total as a floor. **F5 remains open, and the reviewer traced exactly why.** `CancelWhileServingPage` flips the token inside `Respond::respond`, which wiremock calls to *build* the template — strictly before any response byte is written — and `CancelToken::run` is `biased` with the cancel branch first, so **page one's own request is cancelled in flight and the walk never obtains the `Link` header it is supposed to decline to follow.** All four assertions hold under that mechanism just as well as the intended one. It also showed the mutation grain is consistent with *either* reading, so that evidence never discriminated. The concrete fix uses a discriminator already in the file: `headroom` is written **only** in the `Ok(response)` arm, so asserting it is `Some` after the failure proves page one succeeded — and the assertion is expected to **fail today**, which is the point. |
| 2026-08-21 | **The CRLF mutation guard, in its stronger form.** Asserting the target text is present before writing catches only the never-matched case — not a mutation that matched and wrote nothing, nor one that wrote something other than intended. The general rule, cheaper and covering all three: **require `git diff --stat` on the target file to be non-empty between mutating and running the test**, and revert with `git checkout -- <file>` rather than a reverse patch. Content-based, line-ending-agnostic, and it converts a silent no-op from a false green into a hard stop. Supporting habits: drive mutations with a tool that opens files with `newline=''` rather than `perl -0pi`, which is where CRLF keeps biting this repository, and prefer deleting a whole line or statement over rewriting text that spans a line break. |
| 2026-08-21 | Domain closure round merged. Review verified every claim: the new variant's `Display` is true of a dead process and distinct from both neighbours, `RegistrationTimedOut`'s arm and the guard's two pre-existing arms are **byte-identical**, all four sites naming the `e3` closure now name the new variant with no stale promise surviving, and the two guard additions **red independently of each other** — one on the new arm's inner assertion, one on the after-sweep assertion — so neither is redundant cover. `F2`'s compiler notes were reproduced from the parent tree: exactly one `E0004` at `Display::fmt`, then exactly one more at `earliest_state_producing`, then clean. **No third stop.** The serde correction was independently reproduced with different probe strings and the adjudication is that **this ledger's brief was wrong**: an unknown *outcome* tag is a `Data` error carrying a real position, so branching on `classify()` throws it away. |
| 2026-08-21 | **The sharpest finding of the round, and a final small round for it: the best reasoning in this crate is the one thing a future "simplification" would silently undo.** The `line() == 0` discriminator is stated as load-bearing and backed by a measurement table — but swapping it back to `classify()` leaves the **entire suite green**, because the only test covering that path plants an unknown *reason* variant, which is positionless under **both** discriminators. The one row that separates them is never planted. By this crate's own standard — a measurement recorded once and never re-run is a gap — that is the same shape of hole the compiler notes were written to close. Also dispatched: derive the round-trip list from `ALL` rather than hand-maintaining it, since `RegistrationTimedOut` has been absent from it since it was added — **with a substitution that matters**, because `ALL` carries `Other(String::new())` and a naive derivation would *weaken* the one case that test is named for. Correction for the record: the previous report's claim that `cargo doc` "does not pass with four errors" is wrong on both counts — it exits 0 and the four items are warnings, unchanged before and after. |
| 2026-08-21 | `d1` redaction-residue round complete (in review): L1-L6 closed, one file, red-then-fix, gate green on Windows **and** on the real Linux kernel — and the **red commit was verified red on Linux too**, six named failures, not just on the machine that wrote it. **A measured deviation from this ledger's brief, and the worker was right:** the brief listed the backslash among the characters ending a URL span, but including it costs a real redaction — a userinfo credential followed by a Windows path would end its URL at the first backslash, putting the `@` that identifies the userinfo **outside** the span, and `redact_url` echoes what it is given, so the path comes out verbatim. The double-quote already ends the URL in every `Debug`-escaped body, which is the shape the backslash was there for; the reasoning is recorded in the code so nobody re-adds it. **An addition the L1 fix forced on itself:** bounding the URL means the text before it re-enters the scrubber, and for a URL under a credential key that text is a key with an *empty* value — which pass one redacted, giving `token=[redacted]https://evil.example/x`. That is L5's own defect in a new place, fixed in its own commit. **L6 was measured rather than asserted:** applying the exact merge the finding describes reds **exactly one** test, and it is the one the new comment names — the merged pass inspects the non-empty HMAC after the `=`, renders it as a digest prefix, and returns before reaching the `:` that names the header. The old witness would have red nothing. Three residual shapes measured and honestly reported rather than quietly left: an element-**named** short credential in a plist (relevant to `d3`), a short unprefixed credential as an array element under a non-credential key (no key exists to give it away), and a POSIX path used as a URL password (pre-existing, and not a shape anything produces). |
| 2026-08-21 | Domain crate closed. The `line() == 0` discriminator is now defended by a test that can fail: it plants an unknown *outcome* tag — the one input that classifies as `Data` yet carries a real position — and asserts the rendered message keeps the figures serde actually recorded. **Verified by the orchestrator directly rather than by review:** baseline 130 green, and swapping the discriminator back to `classify()` reds exactly one test, the new one. Before this round that same swap left the entire suite green. The round also improved on its brief twice. For the round-trip list it declined the counterfactual it was given — removing a variant from `ALL`, which it **measured** the round-trip test cannot notice (a different test catches that) — and instead broke the round trip for `RegistrationTimedOut` itself with an asymmetric serde rename, then showed the pre-fix hand-written list passes that same defect green. And it measured that the *naive* derivation passes green while silently replacing the suite's only non-empty `Other` payload with an empty one, so the substitution is documented as load-bearing rather than left as a shape a future editor would simplify. `cargo doc` confirmed to exit 0 with four pre-existing warnings, correcting an earlier report that called them errors. |
| 2026-08-21 | **The redaction residue round is held: its review found the round introduced a regression, and it is worse than any leak on the list.** The new URL branch recurses into the scrubber on both the prefix and the remainder, and while the termination argument is correct — every call is on a strictly shorter slice — it is **silent on depth**, and depth is linear in input length. Measured: a message of 3000 URL-bearing items exits `0xc00000fd STATUS_STACK_OVERFLOW` at about 86 KB, where the **base survives 860 KB** because its URL branch returned without recursing. **A stack overflow is not catchable — it aborts the process** — so a large error body, which is attacker-influenceable content, can kill the agent from inside the log sink. Also found, and the reason `d2`/`d3` still wait: `is_url_terminator` recognises **seven** of `STRUCTURAL`'s nine characters, missing `;` and `&`, and because the URL branch runs ahead of the structural cut a URL earlier in the same word swallows the separator that would have cut it — so `Server=https://vault.local/api;Password=hunter2;` and `Environment=API=https://a.com/v1;TOKEN=ghu_…` pass through **verbatim**. Those are `d2`'s and `d3`'s native shapes exactly. **L1 and L3 do not compose**, and the round added `;` to `STRUCTURAL` for connection strings while leaving that path open. The reviewer also measured that the obvious fix is wrong — adding both characters to the terminator set regresses query redaction — and supplied the one that works: restrict the cut to before the first `?`/`#`, verified to close all nine shapes at no cost to the existing suite. Two further findings: a credential key whose value sits in the **next fragment** is never redacted while the comment claims it is (the same false-invariant failure the module itself warns about, and strictly stronger than the residues reported, because here the key *is* present); and adding `;`, `<` and `>` to `STRUCTURAL` widens an existing hole where a structural character **inside** a secret splits it and only the leading piece is redacted. Both deviations were verified correct and left alone. |
| 2026-08-22 | **`a3` complete, group A closed.** Install scripts, npm wrapper, Homebrew tap, channel updates, and the README carrying the permission disclosure — proved by a test that reads the install command out of `README.md` and executes it verbatim under a real `Restricted` policy, with a negative control so it cannot pass vacuously. Its final round also recorded a systematic weakness for a future round: **every "this step must execute X" assertion in the channels tests can be satisfied by a comment** — found when deleting the dry-run push while keeping its explanatory paragraph left the worker's own new assertion green. Two of them were fixed with a comment-stripping reader; the rest remain. |
| 2026-08-22 | **The redaction regression is closed and merged.** The URL branch now iterates over the tail rather than recursing, and the comment covers **depth** rather than only termination — the omission that let an O(n)-deep recursion through. Verified by the orchestrator directly, because this is the one test whose value is that the suite *completes at all*: the guard passes against an 860 KB body in 0.63 s and the platform suite finishes at 104. The worker exercised **8.6 MB**, a hundred times the size that previously aborted, scaling linearly. **It also found a second denial inside its own F1 fix** — a `?`/`#` scan over the whole remaining fragment, correct but quadratic, which ran more than ten minutes on the large body before it was killed. Only the F2 guard caught it, and only because that body was large rather than because the test was designed for it: **a redaction pass that never returns is the same denial as one that overflows.** `;` and `&` now terminate a URL head so the shapes `d2` and `d3` natively produce no longer pass through verbatim, a credential key is carried across a fragment cut, a structural character inside a secret no longer leaves the tail in the clear, and a secret in a URL path is redacted. Green on Windows and on a real Linux kernel. |
| 2026-08-22 | **Run paused at the owner's request.** No new work dispatched after this point; the two workers already in flight were allowed to finish. `d2` and `d3` are now unblocked — the redaction sink they depend on is fixed and merged. Constraints recorded for tasks not yet dispatched, which their briefs must carry because a task specification is immutable: `e1` caches the per-organization repository list and refreshes it on an interval materially slower than the demand poll, and must read `RefreshState::retry_delay` as an absolute floor rather than an addend; `e3` journals terminate-intent **before** signalling and concludes with `TerminatedAfterRegistrationTimeout`, and its DoD must assert that a live unregistered process past its deadline is stopped **before** the attempt is concluded, since `Terminate` and `Conclude` carry identical payload types and the domain cannot refuse the tidy-up that reproduces the defect; `c4` measures demand in **runs**, the matrix under-count being accepted product behaviour; and `f1`/`f2` compute refusals from an activity constant that understates the fallback worst case fourfold. |
| 2026-08-22 | **`c4` implemented and verified, held at `🟣` — merge deliberately not taken.** Branch `worktree-c4-demand-and-jit-gateway` @ `c340212`, four files, 136 github tests, all gates green, scope clean, 23 mutations applied and 23 killed. It is held for two reasons: it is the **only task in this run that did not receive a review round**, and every review in this run found at least one defect that passed silently — so merging it unreviewed would break the one practice that has consistently paid. And it raises a finding that is an owner decision, not an implementation choice. |
| 2026-08-22 | **`c4` found that owner decision (a) has a second consequence, in the opposite direction from the one recorded, and it is the more expensive one.** A workflow **run carries no `runs-on`** — labels live on **jobs**. Without the per-run job listing the decision forbids, **routing-label filtering is not applied to demand at all.** Every queued run in a watched repository therefore counts as demand for **every** policy on it, including runs whose jobs target `ubuntu-latest` or another host's `rm-<host>-…` label. This ledger recorded the accepted cost as an **under**-count — a matrix run of eight jobs getting one runner. That remains true, but it is accompanied by an unacknowledged **over**-count: a repository whose jobs never target this host drives its policy to `max_capacity`, and each JIT ephemeral runner started that way waits for a job it will never be assigned rather than exiting quickly. It is bounded by `max_capacity` and `host_capacity` and travels the accepted surplus-runner path, but at a duty cycle nobody has costed. **This is the orchestrator's to escalate, not the worker's to resolve, and it should be settled before `e1` allocates from these numbers.** |
| 2026-08-22 | Three further `c4` findings recorded for whoever resumes. **The measured demand cost is 1 request per repository per poll, not the estimated 2** — reported through `c3`'s `with_demand_requests_per_repository` seam, which makes three numbers in `04-subsystem-contracts.md` stale (the `~120/hour` demand row, the `~240` total, and "roughly 10 targets per host"). It also opens a reporting contradiction `c4` cannot close from its own files and pinned with a test: `BudgetProjection::admit` sees the seam and would take **13** repository targets, while `max_repository_targets` builds the cost internally, cannot see it, and would print **10** — both numbers appearing in the same `f2` output. The fix lives in `rest.rs`, which `c3` owns. **Two design documents now describe the pre-decision design:** `04-subsystem-contracts.md` still types demand as queued runs "then their jobs, matched against the policy's `routing_labels`", and asks for a `runs-on` must-match/must-not-match table. `b1`'s `RoutingLabels::tally` and the `queued_job` fixtures were built for that path and are now unreachable from the gateway — correct if jobs ever return, but nothing feeds them today. And **zeroise-on-drop is proven one step short**: `scrub()` is called directly and every byte asserted zero, but observing the buffer *after* the drop would be a read of freed memory — a test that proves nothing while appearing to prove everything — so `Drop → scrub` is verified by reading one line, not by a test. |
| 2026-08-22 | **`main` was red, and had been since `a3` merged — four separate defects, now fixed and merged as PR [#2](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/2) (`9f39ffd`).** `a3` was recorded `✅ done` against a suite that never passed CI, and the board asserted the opposite. **(1)** `install/install.sh` was committed `100644`; every `install_sh_*` test but one runs it as `bash install.sh`, where the mode is irrelevant, so only the one test that `exec`s it directly caught it — *Permission denied* on Linux and macOS. **(2)** Seven `install_ps1_*` failures were one missing fixture variable: the Linux and macOS legs run the Windows installer under pwsh, `%PROCESSOR_ARCHITECTURE%` does not exist there, and the suite already fakes `%LOCALAPPDATA%` for exactly that reason but never faked this one. Supplying it keeps the script's own detection line as the code under test — which is why `install_ps1_selects_the_windows_artifact_for_both_architectures`, the one ps1 test that passes `-Arch` and so skips that line, was the only one still passing off Windows. **(3)** The Windows leg's `the_documented_windows_two_step_form...` guarded its negative control with `assert!(env CI is none)` — asserting *a host that cannot lower its execution policy is not a CI host*, which is a property of the runner image, not of the change under test, and is false for GitHub's. The control is kept and sharpened rather than removed: the marker now reports `MachinePolicy`/`UserPolicy`, and the test still fails when no Group Policy explains the refusal. **(4)** `release_channels` had never run on any leg — `cargo test` stops at the first failing binary — and carried a macOS failure of its own: the stand-in `node` is not relocatable, because GitHub's macOS build resolves `@rpath/libnode.*.dylib` beside the executable. It is now a shebang wrapper off Windows. A latent flake surfaced on the way: PowerShell wraps error-record text to console width, so `running scripts is disabled` arrives with a newline inside it depending on the temp path's length. |
| 2026-08-22 | **Owner decision taken on the `c4` demand over-count: accept both counts, decision (a) unchanged.** Demand stays measured in runs, routing-label filtering stays unapplied, and `e1` allocates from these numbers as they stand. The under-count (a matrix run of eight jobs drawing one runner) and the over-count (a repository whose jobs never target this host driving its policy to `max_capacity`, each JIT runner then idling for a job it will never be assigned) are both **accepted product behaviour** as of today. The over-count is bounded by `max_capacity` and `host_capacity` and travels the already-accepted surplus-runner path; its duty cycle remains uncosted, and that is accepted too. No follow-up task is opened. `b1`'s `RoutingLabels::tally` and the `queued_job` fixtures stay unreachable from the gateway by design rather than by oversight. |
| 2026-08-22 | Run resumed after the pause, `--parallel=6 --review=medium --scope=all --merge=on-green`. **The flag buys far less than 6:** the ready set is one task. `e2`'s dependencies are all met, but `e1` holds group E's head and is blocked on `c4`, so the whole group is stalled behind it; `f1` needs `d2`, `g1` needs `f1`, and `h1` needs nearly everything. `d2` dispatched (group D head, and the redaction sink it depends on is merged). `c4` dispatched to a reviewer at `medium` in a dedicated detached worktree — it is the one task in this run that never received a review round, which is why it was held at `🟣` rather than merged. |
| 2026-08-22 | **`c4` reviewed at last, and the review earned its place — then merged as `a671a70` (PR [#3](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/3)), 6/6 checks green.** Six findings at `medium`, all advisory; five fixed before merge, one deliberately not. **The one that justified holding this task:** three of the seven needles in the reservation scan **could never match**. The haystack was lower-cased and the needle was not, so `struct JobLease`, `struct JobClaim` and `struct JobReservation` were un-catchable and the assertion was vacuously true forever — and this is the executable half of the one DoD item otherwise delegated to human review. Its positive control could not notice, because it re-derived the needle the same wrong way and went green; the fix deletes the second spelling rather than correcting it, so scan and control now share one `normalise_for_scan`, and the control plants a `fn`, a type and an acknowledgement shape. `ack` was also missing from the needle list entirely, though the DoD names it. **Second real defect:** a `201` whose body fails to decode was classified non-terminal, so a caller branching on `is_terminal()` retries — and every retry is a *fresh successful registration at GitHub* whose configuration is then discarded, silently accumulating registered runners that never start. Now terminal, with an operator action. Also fixed: the fake gateway and production disagreed about an unreadable repository at **repository** scope (fake said `Ok(total 0)`, production says `Err`) — exactly the "start no runners for a target we cannot see" confusion the production code says it exists to prevent, and invisible because the only test of that path used org scope; a doc comment claiming a tripwire the test did not actually pin, now made true by scanning the module's own production half for `b1`'s label vocabulary; and four rustdoc warnings. Every fix was watched **red before green**. **F6 left deliberately:** `repository_queued` duplicates `c3`'s `repository_in_progress` line for line, but `RestInventory::get`, `MAX_BENIGN_TOTAL_COUNT_SKEW` and `RunsPage` are private to `rest.rs`, which `c3` owns — the risk is the two copies of the skew constant and the two identical tripwire messages drifting apart, and the seam is one `pub(crate)` away whenever `rest.rs` is next open. **Recorded for whoever fixes F3 properly:** `Forbidden` and `Status { 404 }` arriving through the public `#[from]` rather than through `classify` have the same shape, and were left alone for a good reason — GitHub answers a secondary rate limit with a `403`, which is why `classify` runs `RateLimited::detect` first, so calling an unclassified `403` terminal would turn the one 403 that resolves by waiting into a permanent failure. The correct fix is to route them through `classify`, not to grow a second status table. |
| 2026-08-22 | `e1` dispatched — group E's head, unblocked by the `c4` merge. **Its brief carries two constraints its immutable spec cannot**, and the first contradicts a line of the spec's own Scope section: demand is measured in **runs** with no routing-label filtering of any kind, so `demand = queued jobs whose runs-on matches this policy's routing labels` is not what the gateway provides and `e1` must not try to make it so. The accepted under- and over-count both travel the surplus-runner path the spec already calls normal, bounded by the two ceilings `e1` itself enforces. `b1`'s `RoutingLabels::tally` stays unwired on purpose. Second: cache the per-organization repository list and refresh it materially slower than the demand poll, and read `RefreshState::retry_delay` as an absolute floor rather than an addend. `e1` is asked to report which DoD items constraint 1 makes unreachable rather than quietly reinterpreting them. |
| 2026-08-22 | **`d2` merged as `7e0855c` (PR [#4](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/4)) after two review rounds — the full `K = 2` budget, and both earned it.** Seven advisory items in the first round, three substantive: a **disclosed limitation that was wrong in the optimistic direction** (the worker reported that a second Windows operator takes ownership; in fact `std::fs::rename` needs `DELETE` on the target, `BUILTIN\Users` on `%ProgramData%` carries no `DC`/`DE`, so the second `auth login` fails outright with a bare "Access is denied" **after** writing a valid machine-decryptable DPAPI blob to a temp file that was then left behind); the **DPAPI plaintext buffer freed un-zeroed** on the `unprotect` path, where that buffer *is* the token; and the **systemd credential read byte-for-byte**, so a trailing newline becomes part of the token and the resulting auth failure reads as a bad token rather than a bad read. Plus `the_file_is_overwritten_before_it_is_unlinked`, which asserted nothing about the overwrite — the zero-fill could have been deleted with no test failing. **The worker's own first reproduction of finding 1 proved nothing, and it said so:** `MOVEFILE_REPLACE_EXISTING` accepts `DELETE` on the object **or** `FILE_DELETE_CHILD` on the parent, and the test process owned its temp directory, so denying the file alone left the rename succeeding and the test was measuring its own new probe. **The design answer, recorded as product policy rather than an ACL artefact: one machine-scoped credential per host.** The DACL was deliberately *not* widened — an ACE letting a second operator replace the file would let every interactive account read the one credential the product holds; "may B overwrite A's token" is a trust question, and on Windows the answer is spelled *administrator*. The re-review then found the fix's own test **did not discriminate**: the rename path re-derives the identical diagnosis, so removing the pre-write probe left every assertion green and the four-times-stated "before anything is encrypted or written" was asserted by nothing. Closed in the final round with the reviewer's discriminator (deny `Everyone:(WD)` on the directory too), measured both ways — with the probe deleted the new test reds and **the old test stays `ok`**, which is the finding and the fix in one measurement. Also closed: an unwind between the deny and the restore stranded a fixture-token DPAPI blob in `%TEMP%` under an ACL ordinary cleanup cannot remove; now an RAII guard, confirmed by injecting a panic. **Carried forward, not fixed:** `create_protected_file` duplicates ~55 lines of `unsafe` FFI from `d1`'s `process.rs`, which `d1` owns. |
| 2026-08-22 | `e1` and `f1` both implemented, reviewed, fixed, and held at `🟣` pending re-review. **`e1`'s review measured a real host oversubscription** using `e1`'s own `FakeLauncher::forgetful` fixture with two policies instead of one at `host_capacity: 3`, lock held correctly throughout: `started = 6`. Each policy's budget was computed from a set that did not yet contain the other policy's launches, and `e1`'s own test for this exercised a single policy, so the residual was invisible to it. It pairs with a second finding from the opposite side — `attempts()` returning a bare `Vec` left `e3` to answer a journal read failure with `vec![]`, and an empty set is indistinguishable from an idle host, so a transient read error would read as "nothing is running" and the next pass would allocate the whole machine. Both fixed **before `e3` exists**, which is the only cheap moment: `launch()` now returns the attempt and the reconciler merges by `id`, and `attempts()` is fallible with a distinct `AttemptsUnreadable` outcome reporting **no allocation at all** rather than an allocation of zero. Confirmed by injection in both directions. The reviewer separately confirmed the `- active_owned_runners` term is correct, that dropping it reds five tests **without hanging** once the budget bounds the pass, that every negative control can fail, and that `e1`'s reservation scan **inherited `c4`'s fix rather than `c4`'s bug**. |
| 2026-08-22 | **`f1` raises a consequence for `f2` that nothing currently pins, and it is the more expensive half of the budget contradiction.** `f1` was told to navigate the 13-versus-10 disagreement without touching `rest.rs`, and did: it prints **13**, derived from `admit` rather than from a division, pinned by a test that admits targets one at a time until refused. But `BudgetProjection::admit` populates `Admission::Refused { max_repository_targets }` by calling `Self::max_repository_targets` internally (`rest.rs:1625`), so an `f2` that builds its candidate through `c4`'s seam will **decide at 13 targets and print a refusal saying "about 10"** — the contradiction relocated into exactly the place `04-subsystem-contracts.md` says it must not be, the refusal an operator reads. `host show` escapes only because it does not use `Admission`'s `Display`; `repo add` cannot. **The fix is one of two changes in `rest.rs`, which `c3` owns:** set `DEMAND_REQUESTS_PER_REPOSITORY_PER_REFRESH` to `1` (which also reds `demand.rs`'s `the_measured_demand_cost_is_reported_through_c3s_seam`, deliberately), or give `max_repository_targets` a `TargetCost` parameter so the caller supplies the cost it will admit with. `f1` pinned the divergence so whichever lands is visible rather than silent. Also recorded: `f1` reports **five** auth states rather than the specified four, adding `unreachable`, on the argument that folding it into `revoked` would tell an operator with a dropped connection to obtain a new credential. |
| 2026-08-22 | **`e1` merged as `caa447b` (PR [#5](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/5)) and `f1` as `5072a71` (PR [#6](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/6)), both after the full `K = 2`.** `e1`'s second round closed the two residuals its own fix had created: the host ceiling now rests on merge-by-`id`, so `launch()` returning a duplicate `AttemptId` gives five slots on a host of three — documented on the port and caught by a `debug_assert`, deliberately *not* asserted against the launcher's own set, since a launcher honouring read-your-writes legitimately reports the same attempt back; and `scale_down` returned `ScaleDownReport::default()` on an unreadable set, all zeros and indistinguishable from an idle host, which was finding 1's hole surviving in the one place it had not been closed. The load-bearing assertion there is `assert_ne!(blind, ScaleDownReport::default())`, because that equality *was* the finding. The worker also added `#[non_exhaustive]` to a public type mid-round and **reverted it before committing** as scope creep that would not have prevented the mistake anyway. **`f1`'s rounds are the clearest case this run of the loop correcting itself in both directions.** The reviewer's `RateLimited = 4` example did not compile — `#[repr(u8)]` with explicit discriminants makes a duplicate `E0081`, a hard error that never reaches a test — and the worker checked with rustc instead of trusting the brief, then built the case that really slips through: a fresh but *wrong* value, `RateLimited = 2`, colliding with the code clap owns. Pre-fix that ships **39 passed, 0 failed**; post-fix it reds. The reviewer then verified the retraction itself with rustc and rebuilt both halves as reduced models. It also judged the worker's A2 argument **better than its own proposed repair**: repairing the permanently-true `store_agrees_with_start_mode` would have meant probing the other scope's store, i.e. a macOS login-keychain lookup as a side effect of `status`; removal was right, and `credential.store_scope` and `host.service_start_mode` both remain so a consumer can still make the comparison knowingly. **Two product changes came out of tightened assertions and were judged sound rather than assertion-driven:** `Decode`/`Malformed` gained `auth login`, matching the sibling arms, while `UntrustedVerificationUri` correctly still carries none. Final round fixed a message that was wrong at runtime — `auth logout` reporting *"cannot write this sign-in"* — by making the noun bind once per command rather than at every `map_err`, so two nouns in one command is no longer writable, with a bidirectional table test that checks each message carries its own noun **and none of the others'**; one direction alone would have passed the bug. The same loose-assertion defect one function over (`github_failure`, which had **no test at all**) was closed with judgement rather than a bolted-on remedy: `Config` and `app_registration` get the phrase and no remedy, because no command an operator types publishes a GitHub App. |
| 2026-08-22 | **`e2` was terminated mid-commit by a weekly API limit and lost nothing — verified against the repository, not assumed.** Worktree clean, no stash, three commits present and already pushed to `origin/worktree-e2-runner-package-cache` at `fcc42ab`, one of them titled `fix three tests that mutation testing proved could not fail`. Only the pull request was missing. Resumed from that point rather than re-dispatched, so its context and worktree survive. Recorded because the alternative reading — that an agent killed mid-sentence has lost its work — is what leads a resume to reap a live slot, which is the failure this ledger's own tooling was built to prevent. |
| 2026-08-22 | **`e2` merged as `fff4299` (PR [#7](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/7)) after two rounds, and it carried the two worst defects found in this run — both on the extraction path of a `security_critical` task.** The worker arrived having already run **24 mutations across three rounds** and found three of its own tests green under mutations that should have killed them: the "second install is a no-op" test advanced the clock past `check_interval` only, leaving the entry inside the 30-day freshness window so the freshness branch returned early and the short-circuit under test was **never reached**; the workspace-containment test was vacuous *by structure*, because refusal and acceptance were separate tests and gutting `is_inside` to always-true made `lease` refuse every workspace, which satisfies "inside is refused" perfectly; and a path-component property was asserted over one safe input. It also **changed production code to make a test honest** — the partial download used to sit inside the guarded staging directory, where `StagingGuard::drop` removed it regardless, so deleting the real removal would have turned nothing red. **The review then found a fourth**, in the very proof the worker had just repaired: two of the three legs of the no-op test assert a mechanism the production code documents as impossible, since the commit is a single `fs::rename` which cannot replace a populated directory — verified empirically as `DirectoryNotEmpty`, marker intact. It read as triple-proof and was single-proof. **The security findings.** `.tar.gz` extraction called `set_preserve_permissions(true)` with no mask; in tar 0.4.46 that is *weaker than the default*, applying setuid, setgid and world-writable exactly as published, on a package fetched from the network — and because `set_executable` is a no-op off Unix the stated policy was enforced only on Windows and skipped on the two platforms that carry modes. Fixing it exposed a **second, worse defect the review had not named**: `(mode & 0o111) | 0o600`, the formula the review held up as the correct one to copy, **carries the group and other execute bits through**, so a published `0755` left every account on the machine able to execute the runner binaries. `policy_mode` is now owner-only, pure and shared by both paths. **Three retractions, all recorded.** The reviewer overruled the orchestrator's instruction to make tar refuse symlinks — the runner tarball bundles Node under `externals/node*/` and Node ships relative symlinks in `bin/`, so refusing would have made the first real Linux/macOS install a terminal failure, and no escape case was found that validation admits but refusal would stop. It retracted its own F4: an absolute-path entry *does* distinguish this module's guard from the archive crates', tar documents trimming the leading `/`, and the distinguishing fixture was already twenty lines below the comment it had written. And it accepted that its F1 understated the defect. **One measurement honestly not made:** restoring `preserve(true)` only affects `#[cfg(unix)]` assertions, which report *skipped* rather than red on a Windows worktree — stated plainly rather than covered by the other measurements, and ruled adequately covered after the reviewer independently verified the fixture self-check re-reads the *serialised* archive and counted 106 tests with exactly 3 unix-only against the worker's 103/106 leg split. **Carried forward:** `HttpFetcher` and `GatewayCatalog`, the two adapters that touch the network, have zero coverage; `PinnedDigests` has no operator surface, so the remedy `ChecksumAbsent` names cannot currently be performed; `VersionRejected` is unproducible through `GatewayCatalog`; and the `lease`/`prune` TOCTOU is recorded rather than fixed, with the note sharpened to say only `e1`'s host allocation lock closes it. |
| 2026-08-22 | **`d3` dispatched alone, under the owner's explicit decision to allow local privileged tests.** It is the taskflow's only `production_touching` task and takes a host-exclusive resource, so every other slot was drained first rather than running it alongside `e3`, `f2` and `g1`, which are all ready. The brief carries the owner's grant and its limits: it **may** register and remove a real Windows service on this machine to prove `install`/`uninstall`/`status` end to end, under a fixture name that cannot collide with a real installation, self-cleaning on every exit path including panics via an RAII guard rather than a happy-path tidy-up, and it may never touch a service it did not create. It **may not** reboot the host, so the "after a real reboot with no interactive login" bullet is unsatisfiable by it and belongs to human gate 3 — instructed to prove the proxy properties and say plainly that the reboot is unproven rather than dressing the proxy up as the real thing. macOS and Linux cannot be exercised from here at all, so the three backends go behind one seam and Wave 3 validates the other two. |
| 2026-08-22 | **`d3` complete and held at `🟣` — PR [#8](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/8) @ `5ff9c52`, 7/7 checks green including a new `privileged installer smoke tests (windows-x86_64)` job. Not merged: it is the one task in this run that has not been reviewed, and the run stopped before its review round.** Shape: **rendering is pure and `cfg`-free**, so every CI leg asserts every platform's service definition, while applying sits behind a `ServiceControl` trait with a public in-memory double. Windows has no logon-start service — trigger-start covers domain join, IP, device, firewall and group policy but not logon — so `--start-at login` there is a Task Scheduler task at `LeastPrivilege` behind the same trait. **Two defects the tests found that inspection could not.** Task Scheduler takes `RestartOnFailure/Interval` in whole minutes with a one-minute floor and **refuses the registration** rather than rounding (`(39,25):Interval:PT15S`); it now rounds **up**, never down, so "does not restart-loop faster than that bound" stays true, and `service status` reports the difference as a note rather than a mis-registration. And the service account was derived from `cfg!(windows)` rather than from the definition, so a Windows host rendering the launchd plist wrote `UserName = NT AUTHORITY\SYSTEM`, an account macOS does not have — caught only by a cross-platform renderer test. **Eight red-then-green measurements**, including `uninstall` also removing `state/` (fails naming the lost `runner.tar.gz` and `user-access-token`), `inspect_binary` always `Missing` (9 tests fail, including the healthy discriminators), `is_least_privilege()` always true (10 fail), and a `panic!` injected mid-test after install — the test fails and **the machine is clean afterwards**, so the Drop guard holds on the panic path. Restart bound measured at **10.0196112s against a 10s bound** in CI. **Verified by the orchestrator directly on the host after the run:** no service and no scheduled task matching the product name remains, and the worker's own reported slip — a backup file briefly written outside its worktree — is gone, with the main checkout clean. |
| 2026-08-22 | **Three seam gaps `d3` found that belong to other tasks, and they are the most valuable output of this round.** **(1) The daemon and the CLI resolve different application-data directories.** A `LocalSystem` service resolves `%LOCALAPPDATA%` under `system32\config\systemprofile`, and a root LaunchDaemon or systemd unit resolves `/root` or `/var/root`. The *token* is fine — it lives in `%ProgramData%` / `/var/lib` — but **the SQLite database is not, so the daemon would read a different database with no policies in it**. `RUNNER_MANAGER_DATA_DIR` is not the fix: `Context::secret_store` re-roots the store under it and `rooted_site` inserts a scope segment, so the rooted machine store is a *different file* from the one `auth login` wrote. This spans `d1`, `f1` and `f3` and is not `d3`'s to close; `d3` captures all four directories at install time so `service status` can at least report them. **(2) No CLI verb reaches `set_start_mode`** — `ServiceCommand` declares only Install/Uninstall/Status, so requirement 7's "switch without reinstalling" has no command surface until `f3` adds one; `g3` can reach it through the TUI. **(3) `host show` reads `Host.service_start_mode` from SQLite, not from the service record**, and `set_start_mode` only returns the new mode and documents that the caller must persist it — so if `f3` forgets, the two disagree silently. **A decision for a reviewer to weigh:** implementing "and no more" literally on Linux (`ProtectSystem=strict` plus `NoNewPrivileges=yes`) means **the runner inherits the sandbox**, so workflows cannot escalate or write outside the four directories. `07-security.md` assumes hostile workflows so the direction is intended, but it is a real behavioural limit and is documented prominently. |
| 2026-08-22 | **Run stopped at the owner's request. No new work dispatched after `d3`; nothing is in flight; the main checkout is clean and every worker branch is merged or open as a pull request.** Five tasks merged today — `c4`, `d2`, `e1`, `f1`, `e2` — on top of fixing `main`, which had been red on every push since `a3` merged. **State on resume: `d3` needs its review round and a merge decision (PR [#8](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/8), green, unreviewed).** Then the ready set is **three concurrent group heads — `e3`, `f2`, `g1`** — the widest this graph gets; `d3` had to run alone before them because it takes a host-exclusive resource. **Constraints their briefs must carry, since a task specification is immutable:** `e3` journals terminate-intent **before** signalling and concludes with `TerminatedAfterRegistrationTimeout`, and its DoD must assert that a live unregistered process past its deadline is stopped **before** the attempt is concluded, since `Terminate` and `Conclude` carry identical payload types and the domain cannot refuse the tidy-up that reproduces the defect; `e3` must also consume `e2`'s `lease`/`release` port truthfully, because the prune guard is only as truthful as those calls, and run `prune` under `e1`'s host allocation lock, which is the only thing that closes the recorded `lease`/`prune` TOCTOU. `f2` inherits **two** recorded contradictions: `BudgetProjection::admit` decides at **13** repository targets while `Admission::Refused`'s `Display` says **"about 10"** (`rest.rs:1625`, `c3` owns the fix — either set `DEMAND_REQUESTS_PER_REPOSITORY_PER_REFRESH` to `1`, which deliberately reds `demand.rs`'s seam test, or give `max_repository_targets` a `TargetCost` parameter), so `repo add`'s refusal would print a number it did not decide with; and `f1`/`f2` compute refusals from an activity constant that **understates the fallback worst case fourfold**. **The single most useful thing to carry forward: every one of the seven reviews in this run found at least one test that could not fail** — vacuous needles, an overwrite asserted by nothing, a probe whose removal changed no outcome, a completeness check true by construction, two legs of a three-legged proof asserting a mechanism the code documents as impossible — and in the one case where the worker had already found three itself, the reviewer still found a fourth. Briefs from here should keep naming the specific assertions most likely to be vacuous and requiring the red-then-green measurement; that instruction is what produced every one of these finds. |
| 2026-08-22 | **`d3` reviewed, corrected, and merged as `38b178a` (PR [#8](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/8)); all seven checks green.** Low review found three actionable failure-path defects: initial install could leave a live unrecorded registration when `service.toml` persistence failed; start-mode switching removed the working registration before the replacement and durable record were established; and launchd discarded `launchctl enable` failure. Commit `a34314f` made record writes atomic, added compensating rollback for install and mode-switch failures, propagated and compensated launchd enable failure, and added independent failure-injection tests. The reviewer re-ran 220 platform tests plus integrations, 80 targeted service tests, clippy, formatting, and diff checks and verified all three closures. Windows privileged CI passed, including restart-policy and leak cleanup. Per the owner's prior gate decision, native privileged macOS/Linux execution and the real no-login reboot remain explicit later validation evidence; neither is claimed as exercised here. |
| 2026-08-23 | **Parallel round `e3` / `f2` / `g1` ended with all three open and unmerged after the maximum two review-fix rounds.** `g1` PR [#9](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/9) @ `dce5cbf` closed the rendered-hitbox mismatch and wired real journal events into the production TUI, but its explicit mouse-capture DoD proof remains circular: production self-mints `CapturePermit`, and removing the real `execute!(EnableMouseCapture)` while returning the permit leaves tests green. `f2` PR [#10](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/10) @ `8eeb8ad` closed explicit boolean parsing, per-policy label persistence, the 10/5 budget contract, and real repo/org gateway coverage, but its repair test violates `Store::insert_policy`: it returns `AlreadyExists` without an existing row, so a real ambiguous commit leaves `pending` and emits no repair command. Its CI is independently red on all three platforms because the full-command secret-output corpus was not updated for the implemented commands. `e3` PR [#11](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/11) @ `d3b977d` closed direct replacement launches, PID/state crash recovery, normal stop-failure capacity retention, timeout parity, startup gating and terminate-intent directory durability, but the production launcher prunes the newly leased current runtime version, which e2 necessarily rejects as `VersionInUse`, wedging every launch before registration; replacement intents also have no e1 consumer, an unrelated-lock guard can still reach public prune, and identity-write plus stop failure can remain non-durable. All three clean worktrees and remote-matching branches are retained because the PRs are unmerged. No downstream task is ready: `f3` needs both `f2` and `e3`, and `g2` needs `g1`. |
| 2026-08-23 | **Owner-directed recovery resumed for `e3`, `f2`, and `g1`.** The owner explicitly instructed the scheduler to resolve all issues autonomously and stop only for answers or actions only the owner can provide. This authorizes one bounded recovery round beyond the normal two-fix-round cap; it does not relax DoD, independent review, CI, merge-protection, or human-gate requirements. The retained clean, remote-matching worktrees and open PRs are reused. |
| 2026-08-23 | **A3 CI correction dispatched after the recovery batch exposed a shared installer-harness failure.** Product-focused suites and reviews are green, but repository-wide runs are nondeterministic across host/tool combinations: PowerShell crashes with assembly-load/stack-overflow errors on hosted macOS/Linux, while Git-for-Windows `dash` cannot locate its installed `mktemp.exe` locally. A fresh isolated A-group worktree owns making the installer tests deterministic without weakening the checksum, corruption, idempotency, architecture, or destination assertions. |
| 2026-08-23 | **A3 CI correction merged as `e22c922` (PR [#12](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/12)); all seven checks green and independent review clean.** The installer harness serializes only PowerShell child processes, preventing concurrent hosted-runtime crashes while preserving every test body and assertion. Direct Git-for-Windows `dash.exe` now receives its adjacent `usr/bin` on `PATH`, and an anti-vacuity probe requires `awk`, `sed`, `grep`, `cut`, `tr`, `mktemp`, and `tar` before the real checksum/corruption scenario runs. The focused suite passed 25/25 locally across PowerShell 7, Windows PowerShell 5.1, and Git dash; no substantive checksum, corruption, rollback, idempotency, version, architecture, or destination assertion was removed. |
| 2026-08-23 | **Owner-directed recovery completed: `f2` merged as `19a9db0` (PR [#10](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/10)), `g1` as `e97e85c` (PR [#9](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/9)), and `e3` as `a32818a` (PR [#11](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/11)); every final matrix passed 7/7 and every independent review closed clean.** F2 now repairs a genuinely ambiguous SQLite commit, runs authorized success paths in the full-command secret corpus, and discloses the destructive reach of repository Administration permission. G1 preserves Crossterm's native Windows mouse dispatch, pairs focus setup/restore on every exit path, and refuses captured stdio before acquiring terminal modes; the last guard closed a real combined-branch CI hang in which the F2 corpus left three TUI children waiting forever. E3 excludes the leased runtime from pruning, routes replacement intents through ordinary allocation, makes prune authority unforgeable outside the lifecycle seam, bounds post-spawn stop failure, durably marks unresolved live processes, and uses a positive readiness handshake for its cross-platform fixture. The recovery produced no waived finding and no bypassed check. |
| 2026-08-23 | **`f3` dispatched as the only ready task before human gate 3.** Its dependencies `f2`, `e3`, and `d3` are merged. `g2` is dependency-ready but deliberately withheld because the ledger stops Wave 3 dispatch until the Wave 2 Windows pilot records observed capacities and passes the boot-start recovery gate. |
| 2026-08-23 | **`f3` merged as `17b08ea` (PR [#13](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/13)); all seven checks green and the final independent review verified every correction.** The daemon now takes the host lock, loads only active autoscale policies, groups identical targets, gives each target its own demand/contact/backoff loop and package catalog, and drains busy work on shutdown. Review found and closed two multi-target restart defects: aggregate backoff let one offline target delay healthy targets, and flattened startup recovery let the wrong target launcher capture and lose another target's replacement intent. A two-target lifecycle regression now proves target-local recovery and restored host-wide attempt visibility for capacity enforcement. The extra public `service set-start-mode` verb was removed because the immutable F3 and architecture command lists are exhaustive; D3's internal start-mode operation remains for G3's required TUI control. The Windows full check completed in 9m35s; no finding, scope rule, or check was waived. **Human gate 3 is now the only blocker to Wave 3 dispatch:** observed `host_capacity` / policy `max_capacity` approval and a real Windows boot-start pilot with no interactive login remain unproven. |
| 2026-08-23 | **Gate 3 preflight completed read-only on the Windows pilot host.** A fresh current binary reports no runner-manager credential, no host record, no repository or organization policies, and no installed service; GitHub has never been contacted. The safe proxy evidence is already green (native privileged Windows registration/restart/cleanup CI and F3's full Windows suite), but an actual pilot cannot begin until the owner completes GitHub's device authorization, approves measured `host_capacity` and policy `max_capacity` values, and authorizes the real reboot/no-login observation. No capacity is inferred from the default value or runner count. Wave 3 remains undispatched. |
| 2026-08-23 | **Gate 3 live pilot reopened `d3`: the installed production binary is not a valid Windows service host.** Device authorization succeeded; the owner approved and the CLI persisted `host_capacity=1`, repository `max_capacity=1`, routing label `rm-ivanpc-win-x64`, and armed `IvanMurzak/GitHub-Runner-Scaler-UI`. A boot-mode LocalSystem registration was installed and the process proved its data-directory and DPAPI seams by recording a successful GitHub contact. However, SCM then emitted events 7009 and 7000 and stopped it after exactly 30 seconds because `runner-manager.exe daemon run` never connects to the Windows service dispatcher. The privileged CI fixture tests a purpose-built service-host executable, so it did not exercise this production binary seam. Reboot is withheld until an isolated D3 correction makes the actual binary report service state/control and a native privileged regression proves it. Wave 3 remains undispatched. |
| 2026-08-23 | **Gate 3 live pilot reopened `e3`: GitHub Runner 2.336.0 rejects the launch command before registration.** The corrected Windows service remained healthy and contacted GitHub, observed one queued job, and enforced both approved ceilings (`host_capacity=1`, policy `max_capacity=1`) with exactly one active attempt. Its extracted runner then rejected `Runner.Listener run --jit-config-file <path>` as an unrecognized input and reported that it was not configured, leaving the workflow job queued. This is a product compatibility defect in E3's secure JIT handoff/launch seam, not an owner decision. E3 is reopened for an isolated correction and a non-vacuous regression against the supported runner command contract; reboot remains withheld until a real job completes and cleans up. |
| 2026-08-23 | **The owner-requested Node-runtime workflow correction merged as `629a6b6` (PR [#15](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/15)); `a3` is complete again.** Every external action in CI, E2E and release workflows now uses its official Node-24 major as verified against the maintainers' current releases: `checkout@v7`, `cache@v6`, `upload-artifact@v7`, `download-artifact@v8`, and `setup-node@v7`. An exhaustive regression dynamically discovers every workflow YAML file, rejects unaudited/deprecated action refs, and cannot pass with an empty inventory. Independent review found and closed the original fixed-file-list bypass. All seven PR checks passed, including the 9m25s Windows full suite and privileged installer smoke tests; the reported `cache@v4` Node 20 warning is absent on the upgraded head. |
| 2026-08-23 | **The gate-3 E3 correction merged as `10a3e4d` (PR [#16](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/16)); `e3` is complete again.** Authoritative Runner 2.336.0 source confirmed that `jitconfig` is a secret `ACTIONS_RUNNER_INPUT_*` value. The product now starts the listener with the exact bare `run` command and injects `ACTIONS_RUNNER_INPUT_JITCONFIG` only at final child creation; the blob never enters the command line or reusable spawn specification, Runner masks and removes the environment value during parsing, and the restrictive handoff is still deleted immediately. Independent review verified the security and compatibility seams; all seven checks passed after one transient unchanged privileged-test retry. The corrected live service then allocated exactly one ephemeral runner, registered it, executed the queued repository job, and cleaned it completely (`in_use=0`, `active_attempts=0`). The job's own guard failed only because the already-running SCM inherited the boot-time PATH and selected WSL's `bash.exe`, which refuses LocalSystem. Git Bash is installed and has now been placed first in the machine PATH; the authorized gate reboot will both apply that environment to SCM and prove boot-start/no-interactive-login before the clean pilot rerun. |
| 2026-08-23 | **Human gate 3: GO.** The owner-approved values remain `host_capacity=1` and repository `max_capacity=1`; no capacity was inferred. Windows booted at `2026-08-24T02:41:45.500Z`; the production LocalSystem service process was created at `02:42:01Z`, two seconds before the first Explorer shell at `02:42:03Z`, and recorded successful GitHub contact at `02:44:03Z`. E2E run [32679338737 attempt 4](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/actions/runs/32679338737/attempts/4) then queued its Windows leg exclusively on `rm-ivanpc-win-x64`. The manager created exactly one ephemeral runner, reported `in_use=1`, `headroom=0`, and one active policy attempt while it ran, the job succeeded, and the runtime returned to `in_use=0`, `headroom=1`, `active_attempts=0`; the full three-leg run concluded success. The repository variable was restored to `windows-latest` after the pilot. Wave 3 is unblocked. |
| 2026-08-23 | **Wave 3 began:** `g2-tui-screens` dispatched as the only ready group head after human gate 3. Its G-group successor `g3` remains dependency-blocked until G2 merges; H1 remains dependency-blocked on G3. The implementation runs in the isolated `g2-tui-screens` worktree with low independent review and merge-on-green. |
| 2026-08-23 | **D3 reopened after its privileged Windows production-entrypoint test failed intermittently for the second time on unchanged `main`.** Both failures reported that the fixture service registration leaked from `production_daemon_entrypoint_reaches_running_and_handles_scm_stop`, while the immediately following workflow cleanup guard found no fixture registration. The same test passed on intervening runs. This is now reproducible evidence of a test/cleanup timing race rather than a one-off hosted-runner event; D3 runs concurrently with G2 in a distinct conflict group to make the assertion deterministic without weakening the native production-binary coverage or leak guard. |
| 2026-08-23 | **D3 cleanup-race correction merged as `725f5f3` (PR [#18](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/18)); all seven checks green and independent review clean.** Windows `DeleteService` is asynchronous, so uninstall now drops its own handle and waits through visible and `ERROR_SERVICE_MARKED_FOR_DELETE` states until SCM reports true absence, bounded at 30 seconds with unexpected errors propagated. The production SCM lifecycle, graceful stop, terminal cleanup assertion, and separate workflow leak guard remain intact. The privileged job passed all eight native installer tests and the independent no-fixture guard; the Windows workspace suite exercised the new non-vacuous retry regression. |
| 2026-08-23 | **`g2` stopped unmerged after the maximum two review-fix rounds; PR [#17](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/17) remains open at `791071d`.** Round 1 closed four original P1s: the isolated screen model is now wired into the production shell, diagnostics use the canonical credential/JIT redactor, repository detail is visibly rendered, and tables implement an eight-row viewport plus a distinct filtered `NO MATCHES` state. Round 2 added a real repository/runner/workflow snapshot producer and made F5 causally trigger refresh. Final review still found four P1 DoD failures: production Activity is always empty and never receives durable lifecycle outcomes; `Forbidden`, `Failed`, and `Cancelled` inventory states are falsely rendered as network Offline; the unbounded F5 queue can burst the REST budget and make exit synchronously drain queued network refreshes; and sort, runner inspection, and Activity acknowledge remain unwired/no-op. The focused TUI suite is 29/29 green and formatting/clippy pass, but review is blocking and no check is waived. Per `/taskflow-execute`'s two-round cap, G2 is `⛔`; `g3` and `h1` remain dependency-blocked. |
| 2026-08-23 | **Owner-authorized bounded G2 recovery began beyond the normal two-fix-round cap.** The owner explicitly replied "You have the permission, go ahead" after the scheduler named the cap and requested authorization. The retained clean worktree and green-but-unmerged PR #17 are reused. Scope is limited to the four final-review P1s: production Activity data, semantically correct gateway failure states, coalesced/stop-priority F5 refresh behavior, and shipped sort/runner-inspect/Activity-acknowledge interactions. Review, CI, merge protection, and every DoD remain mandatory. |
| 2026-08-23 | **Owner-authorized G2 recovery completed and merged as `f7ef73a` (PR [#17](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/17)); all seven checks green and final independent review clean.** The shipped shell now carries real repository/runner/workflow snapshots and durable Activity outcomes, including distinct idle exit, failure, cleanup, retry, rate-limit and remediation facts. Transport Offline is distinct from permission, failure and cancellation states; diagnostics use the canonical secret redactor. Repository and runner detail, sort, filtering, eight-row viewport, Activity acknowledge/copy, and all 24 screen/state renders are wired into production. F5 uses a mutex-linearized active-plus-one-pending model: key-repeat bursts cancel stale work, coalesce to one latest follow-up, suppress stale publishes, make organization discovery cancel-aware, and give stop priority with joined worker termination. Final evidence included 35 focused tests, 5,000 burst-stress rounds, 100 blocked-preflight shutdown runs, and a deterministic 100-key burst test; no finding or check was waived. `g3` is now ready. |
| 2026-08-23 | **`g3-tui-settings-parity` dispatched as Wave 3's final implementation task.** Its G2 and F2 dependencies are merged. The work runs in the isolated `g3-tui-settings-parity` slot with low independent review and merge-on-green; H1 remains dependency-blocked until G3 merges and human gate 4 is resolved. |
| 2026-08-23 | **`g3` stopped unmerged after the maximum two review-fix rounds; PR [#19](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/19) remains open at `443d3f2`.** The production TUI settings flows, shared atomic full-policy mutation, retained exact drain observation, organization budget-floor presentation, service-mode seam, and production-path tests are implemented; 118 app tests, formatting, and strict clippy pass. Final independent review found one remaining P1 race: the daemon can allocate or complete an attempt after the active-count confirmation check but before the policy-row revision CAS, allowing a disable commit against work the operator did not confirm. The attempt-count predicate and policy revision guard must execute in the same SQLite transaction as the policy update, or an equivalent allocation fence must cover the interval. Per `/taskflow-execute`'s two-round cap, no third correction was started; `h1` remains dependency-blocked. |
| 2026-08-23 | **Owner-authorized bounded G3 recovery began beyond the normal two-fix-round cap.** Scope is limited to closing the final transactional race by checking the confirmed active-attempt count and policy revision in the same SQLite transaction as the full policy update, with deterministic interleaving regressions. Independent review, CI, merge protection, and every DoD remain mandatory. The owner's broader authorization also permits future execution-process recoveries, but does not waive product decisions, credentials, destructive-action approval, or evidence gates. |
| 2026-08-23 | **Owner-authorized G3 recovery completed and merged as `f3930e0` (PR [#19](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/19)); all seven checks green and final independent review clean.** Production settings are wired through AppState, render, keyboard, mouse, and the event loop. Host and policy edits share CLI/domain seams, preserve organization budget-floor semantics, persist cache/service settings, validate complete drafts before mutation, and retain exact drain observations. Policy revision, confirmed active-attempt count, and the full policy update now execute under one SQLite `BEGIN IMMEDIATE` transaction, fencing every allocation/completion writer. Deterministic zero-to-one and one-to-zero interleaving tests prove neither writer crosses the transaction. Final evidence: 131 domain unit tests, 9 domain integration tests, 118 app tests, strict workspace clippy, and the 9m38s Windows full CI leg. No finding or check was waived; `h1` is dependency-ready pending human gate 4. |
| 2026-08-23 | **Human gate 4 remains blocked; legacy retirement is not approved.** Gate-3 run [32679338737 attempt 4](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/actions/runs/32679338737/attempts/4) proved managed Windows routing, ceilings, boot recovery, and cleanup, but the job executed only its input guard: checkout, toolchain, build, and acceptance suite were skipped because the four E2E inputs are absent. It is therefore not a representative migrated workflow under `06-migration-rollout.md`. Read-only inventory found no legacy persistent runner registered to this repository; its nine visible records are manager-named `rm-ivanpc-win-x64` records (one online, eight offline). This Windows host does run many legacy persistent services for other repositories; they are out of scope and must not be stopped, relabeled, or retired for this gate. `h1` remains `🔒` until a representative managed workflow succeeds and the owner records GO; the Wave 3 macOS acceptance requirement also lacks a real macOS host. |
| 2026-08-23 | **A3 reopened in parallel for GitHub Actions' Node 20 deprecation warnings observed in CI run [32675263747](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/actions/runs/32675263747).** The current workflows still use `actions/cache@v4`, and the owner requested the workflow steps be upgraded to current official majors. The audit covers every external `uses:` entry across CI, E2E, and release workflows, verifies each candidate against the action's official latest release and migration notes, and must preserve all trigger, permission, concurrency, checksum, artifact, release, and skip semantics. It runs concurrently with D3 because A and D are distinct conflict groups. |
| 2026-08-23 | **A3 action-runtime correction paused before its first edit after preflight exposed a real file conflict with the active D3 correction.** D3 legitimately extends `.github/workflows/ci.yml` so the privileged job builds and exercises the production service entrypoint; A3 must update the same file's action majors. Although A and D are normally distinct conflict groups, this correction crosses that planned boundary. A3 is returned to pending with its clean worktree retained and will be refreshed/resumed immediately after D3 merges. |
| 2026-08-23 | **D3 gate-3 correction merged as `5cc7160` (PR [#14](https://github.com/IvanMurzak/GitHub-Runner-Scaler-UI/pull/14)); all seven checks green and independent re-review clean.** The production Windows binary now enters SCM on the main thread only when a boot-registration marker is present, reports RUNNING/STOP_PENDING/STOPPED, and converts STOP/SHUTDOWN into the daemon's existing graceful drain. Windows login mode omits that marker and runs normally under Task Scheduler. Native privileged tests install and execute the actual shipping binary in both modes, hold boot beyond SCM's former 30-second timeout, prove login remains alive rather than exiting 14, stop cleanly, and assert no fixture registration survives. Review caught and closed the login/SCM cross-wire and an LF-only CI assertion; fresh Linux, macOS, Windows, privileged Windows, and three E2E checks all passed. The live pilot service was not touched during implementation. A3's clean worktree was fast-forwarded to this merge and its action-runtime correction resumed. |
