# Managed WSL host tasks

Tasks are immutable implementation contracts. Progress lives only in the parent
`ROADMAP.md`.

Groups are conflict domains: A owns platform WSL process/task primitives; B owns
the app command surface and runs sequentially. Release and workstation adoption
remain owner-authorized ROADMAP gates after all source tasks are green.

